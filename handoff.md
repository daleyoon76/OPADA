# Handoff — Hometest_op

(각 세션 종료 시 아래 형식으로 항목 추가. 최대 10개 유지, 초과 시 가장 오래된 항목 삭제)

---
## [push 직전 최종 재판정 — 3차 반영분] 2026-09-08 04:42 KST

### 완료
- **[리뷰] `18f6d0b..6497261` 최종 재판정 — 조건부 GO(push 가능)** — 보고서 `team-reports/2026-09-08-opada-checklist-review/final-rejudge.md`. 소실된 재판정 세션을 대신한 별개 검증자
- **[①] `5281b17` 동작 재현** `[관측]` — 항목만 2/23 · 중간안 10/23 · 무가드 16/23 · 화면 위양성 0/0/0 · 정당 0/3/3. 커밋 메시지 표와 전건 일치
- **[①'] 제품 판단 — 맞다** `[관측]` — 표본 01 을 프로덕션 `local_ai_coach` 에 통과시켜 화면 문구 관찰. 표 제출방법 실측값이 `['직접제출']` 이라 안내가 원문 사실에 근거한다. 금지표현 위반 없음
- **[②] B7·S1 무증명 아님** `[관측]` — 3차 세션 변이를 쓰지 않고 자작 변이로 재검증
  - B7 `b_invert`(가드 반전) rc=1 · 116/119 · 양성/음성 동시 붉음 · `b_wrongkey` rc=1 · 118/119. 둘 다 화면 노출 `[1,2,3]`→`[]` 로 비등가 확인
  - B7 `b_all`(any→all)은 rc=0 이나 **표본 결과 불변인 등가변이**임을 확인 — 검사 공백이 아니다
  - S1 `s_swap`(A/B 색 뒤바꿈) → `2 failed`. 렌더 클래스가 `badge safe` 로 실제 변화
- **[③] P1-A 전파 누락 없음** `[관측]` — 옛 값·옛 정의 양쪽 grep. `src/`·`to-do.md`·`changelog.md` 0건, `handoff.md` 5건은 전부 정정 서술
- **[④] P2-C 정정 수치 확인** `[관측]` — 매각/공매 3 · 임대 3 · `처분방식 확인` 17. 3차 세션 정정이 정확
- **[⑤] 헤드라인 재현** `[관측]` — 10/23(A 8/8 · B 2/13 · C 0/2) · 59/59 · 첨부 공고 20 · 순수 게이트 **119/119 · 축 16** · 화면 2/2 · 조건 축 10
- **[⑥] 느슨해진 자리 없음** `[관측]` — 삭제 5줄 전수 확인, 단언 삭제 0건. `check()` 106→111 · `await expect` 42→48

### 자작 종료 조건 (내가 만진 델타)
- **②용** 등가변이 판별 — 붉지 않은 변이(`b_all`)를 표본 23건에 돌려 결과 불변임을 확인했다. 「초록 = 공백」으로 단정하지 않기 위한 조건이다 → **PASS**

### 미완료 / 미검증
- **P2-D `changelog.md:53`** 이 「114단언·15축」으로 다시 낡았다 — 실측 **119단언·16축**. `5281b17` 이 B7 을 넣으며 전파를 빠뜨렸다. 종료 조건: `grep -n "114단언\|15축" changelog.md` 0건 + 게이트 출력과 일치
- **push 안 함** — 판정만 냈다. 커밋 `6497261` 은 여전히 로컬
- **라이브 미검증** — 전부 저장 HTML 23건 기준. 라이브에서 `is_sale=True` 가 늘면 위양성도 늘 수 있다 `[추론]`
- **API 6종 · 실 URL e2e** — 인증키 부재 · 네트워크 의존으로 이번 회차 미실행

### 불안정 영역
- `has_direct_submit` 판정식이 넓어졌다(2→10). 경계는 B7 음성 대조가 고정하지만 라이브 분포는 미측정이다
- `disposition` 파싱이 저장 HTML 에서 17/23 을 `처분방식 확인` 으로 떨어뜨린다. 코치 분기 선택에 직결되나 이번 diff 범위 밖이다

---
## [3차 반영 — 중간안 채택 · 배지 색 단언] 2026-09-08 04:35 KST

### 완료
- **[③] `has_direct_submit` 중간안 채택** — 커밋 `5281b17`. 앞 세션이 워킹트리에 남긴 수정을 그대로 살렸다(문법·의미 온전 확인 후 착수, 되돌리지 않았다)
  - 근거는 항목의 `method` 우선, 온비드 표 `tableRows` 의 제출방법 칸은 **서류 1건 이상일 때만** 보조 인정
  - 표본 23건 재계측 `[관측]` — 항목만 2/23 · **중간안 10/23** · 무가드(옛) 16/23. 화면 위양성 0/0/0 · 화면 정당 0/3/3
  - `is_sale` 은 `build_notice()` 의 `disposition_label` 유도식을 그대로 옮겨 계산했다. 표본의 `is_sale=True` 는 01·02·03 뿐
- **[③] 게이트 축 B7 신설** — A형 실형태(서류 1건↑ + 항목 `method` 없음 + 표 직접제출)를 잰다. 도달 증거 3건을 함께 단정
  - 킬러 변이 2건 `/tmp/mut3a`·`/tmp/mut3b` — 표 보조 근거 삭제 rc=1(118/119) · 항목 가드 삭제 rc=1(117/119)
- **[④] 화면 S1 배지 «색» 단언 추가** — 커밋 `c094614`. A형 `safe` · B형 `warn` 양쪽과 각각의 음성 대조
  - 킬러 변이 양방향 `[관측]` — `safe` 고정 `/tmp/mut4` rc=1(B형 붉음) · `warn` 고정 `/tmp/mut4b` rc=1(A형 붉음)
- **[⑤⑥] `to-do.md` 갱신** — 커밋 `fd88ffc`. 중간안 「채택함」 + 세 안 값·화면 분해·되돌릴 해시. P2-C 는 항목만 신설(코드 미변경)
- **[①②] 앞 세션 커밋 `3b94de1` 의 종료 조건 직접 실행** — 「표본 09·10·18·21·22·23」 `src/`·`to-do.md`·`changelog.md` **0건**(handoff 과거 블록 2건은 같은 줄에 「계산 경로 · 귀속 오류」 단서 동반) · 「99단언/킬러 변이 39」 changelog **0건**

### 자작 종료 조건 (내가 만진 델타)
- **③용** `/tmp/exit_cond_3.py` — 표본 **원본 HTML 23건**을 프로덕션 `local_ai_coach` 에 통과시켜 화면에 「직접제출 서류」가 뜨는 표본이 `[1, 2, 3]` 인지 본다 → **PASS · rc=0**. 게이트 B7 은 합성 픽스처라 이 조건이 실 공고 형태를 대신 잰다
- **④용** `warn` 고정 변이 `/tmp/mut4b` — `safe` 고정만으로는 A형 쪽 단언이 무증명이라 반대 방향을 따로 걸었다 → **rc=1 · A형 테스트 붉음**

### 미완료 / 미검증
- **P2-C 처분방식 파싱** — 코드 미수정. 재판정의 「20/23」을 실측으로 정정해 기록만 했다(`처분방식 확인` 17 · `임대` 3 · `매각/공매` 3)
- **동원님 판단 2건** — `foreign` 조건 축 존치 · CI 도입
- **push 안 함** — 이번 세션 커밋 3건(`5281b17`·`c094614`·`fd88ffc`)은 로컬에만 있다
- **라이브 미검증** — 판정은 전부 저장 HTML 23건 기준이다. 실제 온비드에서는 `dspsMthodNm`·배지가 채워져 `is_sale=True` 가 늘 수 있다
- **`changelog.md` 미갱신** — 이번 3차 반영분은 아직 changelog 에 넣지 않았다

### 다음 세션 시작점
- push 여부 결정 → push 전 재판정 1회(코드 행동 변경이 `5281b17` 1건 있다)
- `changelog.md` 에 3차 반영분 항목 추가
- P2-C 처분방식 파싱 착수 여부 결정

### 불안정 영역
- 게이트의 「미커버 0개」는 여전히 **자기 선언**이다. 재판정이 실측으로 짚은 2건(배지 색 · 실 공고 형태 코치 배선)은 이번에 닫혔지만, 다음에 또 실측으로 세야 한다
- `disposition_label` 유도식을 계측 스크립트가 **복제**하고 있다. `build_notice()` 가 바뀌면 계측이 조용히 낡는다 — `/tmp` 스크립트라 레포 게이트가 잡지 못한다

---
## [2차 반영분 재판정 — 조건부 GO] 2026-09-08 01:00 KST

### 완료
- **[재판정] `4aedfb7..18f6d0b` 독립 재판정 1회** — 보고서 `team-reports/2026-09-08-opada-checklist-review/rejudge.md`(커밋 `94dd41a`)
  - 1차 리뷰어 세션이 소실돼 별개 검증자가 그 자리를 대신했다. 읽기 전용 — 코드·문서 수정 0건, 보고서 1개만 신규
  - 변이·계측은 전부 레포 밖 사본(`/tmp/rj` · `/tmp/rj-old` · `/tmp/mut_*`)에서 수행. 종료 시 `git status --porcelain` 빈 출력
- **[종료 조건] 1차 리뷰의 변이 4개를 자작 변이로 4/4 rc≠0 확인** — 2차 세션 스크립트는 쓰지 않았다
  - 비등가까지 확인한 것 3건 — P1-1 표본 10/23→11/23 · P1-2 깨진 다운로드 URL 생성 · P1-3 표본 18·19 headline 변경
  - P1-4 는 게이트만 붉고 **표본 결과 불변**(1차 리뷰 판정과 동일)
  - P1-3 앵커는 `src/server.py` 에 466·472행 두 곳이라 **466행 한정**으로 넣어야 한다
- **[지표] 헤드라인 전부 재현** — 10/23(A 8/8 · B 2/13 · C 0/2) · 59/59 · 공고 단위 20/20 · 게이트 114/114 · 화면 2/2 · 조건 축 10(코드=주석=changelog 3곳 일치)
  - `samples.json` 관측값 경로도 발동 확인 — 1차 리뷰가 미검증으로 남긴 항목이 닫혔다
- **[구조] 단언 삭제 0건 확인** — `tests/` 의 삭제 라인은 전부 docstring 이며 같은 내용이 런타임 출력으로 옮겨졌다. 「이제 실패할 수 있게 된 것」 4건과 「옆에 새 검사만 생긴 것」 0건을 갈라 적었다

### 미완료
- 🔴 **P1-A 신규 — `has_direct_submit` 축소가 표본 기준 순손실이다.** 화면 출력을 직접 관측한 결과
  - 「위양성 6건」(표본 09·10·18·21·22·23)은 전부 `is_sale=False` 라 **옛 코드에서도 화면에 뜬 적이 없다**. 닫은 것은 계산 경로다
  - 반대로 표본 01·02·03(압류재산)은 화면에 뜨던 **정당한 안내**였고 현행에서 사라졌다
  - `src/server.py` 주석의 「표본 09·10·18·21·22·23 실측」 **귀속이 틀렸다** — 정정 대상
- 🔴 **P1-B — 중간안 채택 여부는 동원님 판단.** 실측은 현행 2/23 · 중간안 10/23 · 옛 16/23이고, 화면 노출 기준으로는 위양성 0/0/0 · 정당 0/3/3 이다
- **P2-A `changelog.md:53`** 이 「99단언 · 킬러 변이 39개 전부 붉어짐」으로 낡았다(실측 114단언 · 그 변이 주장은 1차 리뷰가 재현 불가로 판정한 것)
- **P2-B 화면 축 표 S1 의 「커버 Y」가 과대** — 배지 색은 여전히 무증명(1차 P2-3 미해결)
- **P2-C 범위 밖 기록** — 표본 23건 중 20건이 `처분방식 확인` 으로 떨어져 코치가 임대 분기를 탄다. 이번 diff 무관

### 다음 세션 시작점
- P1-A 주석 귀속 정정 · P2-A changelog 갱신 → 그 뒤 push
- 동원님 판단 3건 — `has_direct_submit` 중간안 · `foreign` 축 존치 · CI 도입

### 불안정 영역
- **판정은 저장 HTML 23건 기준이다.** 라이브에서는 `dspsMthodNm`·배지가 채워져 `is_sale=True` 공고가 늘 수 있고, 그러면 P1-A 의 손익 계산도 달라진다
- 게이트 「미커버 0」은 자기 선언이다. 실측 미커버 2건(화면 배지 색 · 실 공고 형태의 코치 배선)이 남아 있다

---
## [독립 리뷰 P1 7건·P2 6건 반영 — 조건부 GO 해소] 2026-09-08 00:43 KST

### 완료
- **[코드] 제품 동작 2건** (`src/server.py` · 커밋 `0671bfc`)
  - P1-5 용어 안내에서 처분방식 사이 금액 비교 제거 — 「임대」의 「최저입찰가격과 보증금 구조는 매각과 같습니다」와 이를 전이받던 「대부」의 「임대와 같은 구조입니다」
    - 🔴 **리뷰가 「원문 근거 없음」이라 한 부분은 사실과 다르다.** 회의록 265행에 정인기 차장 발언이 있다. `to-do.md` 의 출처 기재(2026-09-07 회의록)는 정확해 고치지 않았다
    - 그래도 축소한 이유는 근거 부재가 아니라 오독 위험이다 — 임대는 사용료·매각은 매매대금이라 금액 규모를 같게 읽힌다
  - P1-7 `has_direct_submit` 의 근거를 `tableRows`(구분명 행)에서 `docChecklist["items"]` 로 이전. 서류 0건으로 「못 뽑았습니다」를 띄운 공고에서 바로 다음 줄이 「직접제출 서류」를 전제하던 결함
- **[검사] 무증명 가드 4건 고정** (커밋 `ef05f64`) — P1-1 license 축 제외 · P1-2 빈 파라미터 앵커 · P1-3 C형 headline · P1-4 물림 하위항목 제한
  - P1-2 는 기존 음성 대조가 정규식 앞에서 걸러져 **분기에 도달하지 못했다.** 5인자 형태를 갖추고 1·5번 인자만 빈 케이스로 교체하고 도달 증거를 함께 뒀다
  - 자작 종료 조건 2건 추가 — P1-5·P1-7 은 리뷰의 변이 4개가 덮지 않는 내 델타다
- **[검사] P2-6 `PLAYED`·`AXES` 를 실행 stdout 으로** (커밋 `ef05f64`) — 두 게이트 모두 상수로 옮기고 연기한 것의 «이름»·축 표·미커버 축 이름을 찍는다
- **[계측] P1-6 부분 표본 위양성 차단** (커밋 `bd5ece4`) — 헤드라인에 「⚠️ 부분 측정 — 표본 N/23만 존재」를 박고 rc=3. 실측 부분 3건 rc=3 · 전수 rc=0 · 디렉터리 없음 rc=2
- **[러너] P2-8 `npm run test:unit` 추가** (커밋 `076af77`). CI 는 만들지 않았다 — 도입은 동원님 결정
- **[문서] P2-1·P2-2·P2-4·P2-5** (커밋 `9302cd4`) — 내역 합 22→23 · 분모를 공고/파일 단위로 분리 · 삭제된 함수 포인터에 각주 · 조건 축 9→10 전파
- **검증 실측** — 순수 함수 게이트 **99/99 → 114/114**, 화면 2/2, 실 URL e2e 1/1(`3 passed`). 종료 조건 변이 4개 **전부 rc=1**, 자작 변이 2개도 rc=1. 변이는 전부 레포 밖 사본(`/tmp/opada-*`)에서 수행했고 종료 시 `git status --porcelain` 빈 출력
- **표본 원자료를 재부팅에 견디는 위치로** — 동원님이 `~/Documents/Dev/.opada-survey/` 에 옮겼다. 이 배치에서 `samples.json` 관측값 경로가 **실제로 발동**한다(「재산유형 출처: samples.json 관측값 · 사본 표와 일치」). 리뷰가 미검증으로 남긴 항목이 닫혔다

### 미완료
- 🔴 **`has_direct_submit` 가 표본에서 16/23 → 2/23 으로 줄었다.** 줄어든 14건 중 8건(표본 01~08)은 서류를 실제로 뽑았고 표의 제출방법 칸에 「직접제출」이 있던 공고다 — 그 8건에서 안내 항목이 함께 사라졌다. 중간안(서류 1건 이상일 때만 표도 근거로 인정)을 `to-do.md` 에 남겼다. **동원님 판단 사항**
- **P2-3(배지 색 변이 생존)·P2-7(외국인 축 표본 근거 0건)은 이번에 닫지 않았다.** P2-7 은 축 존치가 제품 판단이라 판단 재료만 `to-do.md` 에 적었다
- 직전 세션 미완료 유지 — 첨부 PDF·HWP 본문 읽기 · 정답 서류 목록 10건 · 공공데이터 API 경로(`ONBID_API_SERVICE_KEY` 부재) · 박선호 차장 참조 누락 · 발표 슬라이드

### 다음 세션 시작점
- 동원님 판단 2건을 받는다 — `foreign` 축 존치 여부, `has_direct_submit` 중간안 채택 여부
- 그다음은 직전 세션 시작점 그대로 — 첨부 파서 도입 여부 결정 → B형 13건

### 불안정 영역
- **표본 실측 10/23 은 이번 수정으로 움직이지 않았다** — 제출서류 추출 경로는 건드리지 않았다. 움직인 것은 `has_direct_submit`(AI 코치 안내 항목)뿐이다
- 표본 원자료는 **마스킹되지 않은 원본**이다. `~/Documents/Dev/.opada-survey/` 에 두고 레포 안으로 복사하지 않는다
- 리뷰의 P1-5 근거 판정이 한 건 틀렸다(위 참조). **리뷰어가 `[미확인]` 으로 남긴 항목은 그대로 사실로 옮기지 말 것**

---
## [캠코 회의 피드백 코드 반영 · 제출서류 추출 재작성 · 참고용 경계 고정] 2026-09-08 00:15 KST

### 완료
- **제출서류 추출을 절 경계 기반으로 재작성** (`src/server.py`)
  - `split_sections()` 로 `<h2 class="tit">` 절을 나누고 서류명 탐색을 `공고문`·`제출서류` 로 한정
  - 온비드 「입찰방법」 절 공통 도움말은 **절 배제 + 툴팁 제거** 두 겹으로 막음(표본 23/23 전건 오탐 원인)
  - 재산유형으로 A/B/C 사전 판정 — 압류·국유=A, 공유·기타일반·수탁=B, 파산=C
  - 조건 축 10개 분기(대리·공동·**개인**·**법인**·업종자격·미성년자·외국인·사용인감·컨소시엄·**지역제한**)
    - <sub>2026-09-08 정정 — 최초 기재는 「9개」였다. 「개인/법인」을 1축으로 세었으나 코드는 `corp`·`person` 2축이다. 실측 `len(CONDITION_AXES) == 10`</sub>
  - 부가조건(단계·원본/사본·부수·유효기간·제출방법·**서류별 제출기한**)과 발급처 안내를 서류마다 부착
  - 🔴 **못 뽑으면 못 뽑았다고 말한다** — 구분명만 있는 표를 추출 성공으로 세지 않음. `local_ai_coach` 의 `has_docs`·`has_proxy_or_joint`·`has_direct_submit` 를 `docChecklist` 근거로 전환
- **표본 23건 실측(변경 전 → 후)**
  - 구체 서류명 1건 이상 추출: **0/23 → 10/23** (A형 8/8 · B형 2/13 · C형 0/2)
  - C형 0/2 는 정답이 「없음」이라 맞는 값이다. B형 11건은 `attachment_only` 로 정직하게 표시
  - 첨부 다운로드 파라미터: **0/20 → 59/59 파일**(첨부가 있는 공고 20건). `fn_chkPdfRead` 5인자 → `dnldFile.do`
- **회의 피드백 반영** — ① 참고용 경계(진행 차단 지점 전수 조사 후 서류 기반 게이트 0건 확인, 화면에 「참고용·막지 않음」 상시 노출) ② 최저입찰가격 「비공개」(`lowstBidPrcHideDivCd != '0001'`) ③ 용어 안내 분리 ④ 마감 D-day(알림 발송은 미구현 명시) ⑤ 지역 제한 조건 축 ⑥ 비용은 원문값만, 세율 미계산 ⑦ 공고문 절 항목 분해
- **검증 3축** — 순수 함수 `tests/test_docs_extraction.py` 99/99 · 화면 `tests/screen.spec.js` 2/2 · 실 URL e2e 1/1. 킬러 변이 **서버 22개·화면 17개 전부 붉어짐**
- 런타임 관찰 3건 — 옛 압류 샘플(A형 16항목·5조건그룹) · 비타민스테이션(`0` → `비공개`, B형 attachment_only, 첨부 6건 링크 6건) · 상암동(`63,053,628` 유지)
- 커밋 5건(푸시 안 함): `8f5ff27` 서버 · `f4b4fac` 순수 함수 게이트+계측 · `f45f215` UI · `66ca6df` e2e · 문서

### 미완료
- 🔴 **첨부 PDF·HWP·HWPX 본문 읽기 미구현** — B형 13건의 실제 목록은 첨부 안에 있다. 이 레포는 외부 의존성이 없어 파서를 넣으려면 승인 필요. 그래서 B형 추출은 2/13에 머문다
- **정답 서류 목록 미작성** — 표본조사 §6-1이 지정한 10건. 추출 성능의 기준선이 아직 없다
- **공공데이터 API 경로 검증 불가** — 이 기기에 `ONBID_API_SERVICE_KEY` 없음. `lowstBidPrcIndctCont` 폴백은 코드에만 있고 미검증
- 표본 원자료(HTML 23건·14MB)는 `/tmp/onbid-survey/` 에만 있다. 재부팅하면 사라지며 `measure_sample_23.py` 는 그때 건너뛴다
  - <sub>2026-09-08 00:43 갱신 — 동원님이 `~/Documents/Dev/.opada-survey/`(`html/` + `samples.json`)로 옮겼다. 계측은 `ONBID_SAMPLE_HTML_DIR` 로 그 경로를 준다</sub>
- 직전 세션 미완료 유지 — 박선호 차장 참조 누락 · 발표 슬라이드 · 온비드 자료실 매뉴얼

### 다음 세션 시작점
- 첨부 파서 도입 여부를 먼저 정한다(동원님 결정). 도입하면 B형 13건이 열리고, 안 하면 B형은 「첨부를 보세요」 안내가 상한이다
- 표본 10건 정답 목록 작성 → 현재 추출기의 정밀도/재현율 측정

### 불안정 영역
- 서류명 어휘가 **닫힌 목록**이다. 목록 밖 서류는 조용히 빠지는 게 아니라 「못 뽑음」으로 나오지만, A형 공고의 서류를 일부만 뽑을 위험은 남는다
- 조건 물림(`carried`)은 하위 항목 표식(`- ① ㅇ` 등)에만 적용된다. 표식 없이 줄만 바뀐 공고에서는 조건이 붙지 않는다
- `measure_sample_23.py` 의 재산유형은 표본 번호로 고정한 값이다. 공고상세 HTML에는 `scrnCltrPrptDivNm` 이 없다

---
## [제출서류 표본조사 23건 · A/B/C 분류 · 현행 추출로직 공백 실측] 2026-09-07 19:45 KST

### 완료
- **표본 23건 수집** — 진행 중 공고. 압류3·국유5·공유6·기타일반6·파산2·수탁1 / 매각14·임대9
  - 공공데이터 API 아닌 **온비드 공개 페이지 크롤링**(이 기기에 `ONBID_API_SERVICE_KEY` 없음)
  - 목록 엔드포인트를 새로 확인: `PbancClgInqController/inqSzrPrptPbancClg.do` (POST · JSON)
    `srchPrptType` 코드 — 압류0007·국유0010·공유0002·기타일반0005·수탁0008·파산0013. 날짜를 안 주면 최근 1개월로 좁혀져 0건이 나온다
  - 공고상세는 `pbctCdtnNo` 없이도 200. `cltrPrptDivCd`·`cltrScrnGrpCd`·`onbidCltrno`·`onbidPbancNo`·`pbctNo` 5개면 열린다
  - HTTP 요청 **70회**(간격 1.2초 강제), 첨부 다운로드 **27개**(상한 30 준수)
- 🔴 **판정 결과 — 재산유형과 정확히 일치, 예외 0건**
  - **A형 8건(35%)** 본문만으로 특정 가능 — 압류재산·국유재산(**캠코가 직접 집행**)
  - **B형 13건(57%)** 목록이 첨부파일 안 — 공유·기타일반·수탁(**이용기관이 작성**)
  - **C형 2건(9%)** 본문·첨부 어디에도 없음 — 파산자산
  - 캠코가 말한 「규칙으로 잡기 어렵다」의 실체는 재산유형이 아니라 **작성 주체**다
- **첨부 26개 전부 텍스트 추출 성공** — 열지 못한 파일 0건
  - 확장자 분포(전체 59개): pdf 34 · hwp 16 · hwpx 7 · zip 2. doc/xls 0건
  - `.hwp` 1개가 실제로는 HWPX(zip) — **매직바이트로 분기해야 한다**
  - 🔴 `hwp5txt` 는 **표를 `<표>` 자리표시자로 날린다**(9개 파일 90개). 개인/법인 구비서류표가 통째로 소실 → `hwp5html` 로 복구
  - PDF 2건은 pdfminer `PSEOF` 실패 → `pypdf(strict=False)` 로 복구
- 🔴 **현행 `src/server.py` 실측(수정 없음, 저장 HTML에 실제 실행)**
  - `extract_required_docs` — **구체 서류명 출력 0/23**. 내역은 빈 결과 8건 · 구분명만 14건 · 「(공고문 확인)」이 붙은 구분명 1건(`pbanc_23`)으로 합 23건
    - <sub>2026-09-08 정정 — 최초 기재는 「빈 결과 8 + 구분명만 14」로 합이 22였다. `pbanc_23`(`입찰참가자격 증빙(공고문 확인)` 등 3행)이 빠져 있었다. 이 1건도 사람이 읽으면 구분명이라 「0/23」 판정 자체는 바뀌지 않는다</sub>
  - `extract_related_docs` — 파일명은 **20/20 정확**, 다운로드 파라미터는 **0/20**(파일을 열 수단이 없다)
  - `local_ai_coach` — 15건이 `has_docs=True` 로 통과. **추출 실패가 화면에 드러나지 않고** 성공한 것처럼 보인다
- 🔴 **온비드 「입찰방법」 절 공통 도움말이 23/23 전 표본에 「위임장·대리입찰신청서·공동입찰참가신청서」를 포함한다.** 섹션을 분리하지 않으면 키워드 추출기가 전건 오탐
- **산출물 2건 커밋** `250277c` (푸시 안 함)
  - `docs/90_MVP개발/07_제출서류_표본조사_20260907.md` — 리포트
  - `docs/90_MVP개발/source-notes/onbid_제출서류_표본_20260907.md` — 원자료(개인정보 마스킹)

### 미완료
- **정답 서류 목록 미작성** — 사람이 작성할 표본 10건을 리포트 §6-1에 지정만 했다(01·04·12·13·09·15·22·21·23·19)
- **첨부 텍스트 전문(26개)은 커밋하지 않았다** — `/tmp/onbid-survey/txt/` 에만 있고 재현 절차는 리포트 부록 A
- 코드 수정 후보 4건은 **제안만** 하고 착수하지 않았다(리포트 §6-3). 착수 전 승인 필요
- 직전 세션 미완료는 그대로 유효 — 미커밋 `src/server.py`(물건관리번호 형식 변환) · 본선준비 문서 7건 · 박선호 차장 참조 누락 · API 66필드 미반영 · 발표 슬라이드

### 다음 세션 시작점
- 표본 10건에 대해 사람이 정답 서류 목록 작성. 스키마 후보는 리포트 §6-2
  - **단계(입찰전/낙찰후/계약시) 칸을 반드시 둔다** — 표본 6건의 목록이 계약 단계 서류였다
- 그 다음 `extract_related_docs` 에 다운로드 파라미터 추가(가장 작고 확실 · 20/20에서 앵커가 이미 잡힘)

### 불안정 영역
- **표본 23건은 층화추출이 아니다.** 재산유형별 목록 상위 N건이라 전체 분포 추정치로 쓰면 안 된다
- 미다운로드 첨부 33건(감정평가서·도면·zip 등)에 서류 목록이 없다는 것은 **파일명 기준 판단이지 확인이 아니다**
- A/B/C 판정자는 1인. 재현성 미검증
- 물건상세(`mvmnCltrDtl.do`) 페이지는 이번에 보지 않았다. 공고상세만 조사했다
- 전 과정 비로그인. 로그인 후에만 보이는 서류 안내가 있는지 미확인
- 이번 세션은 `src/`·`package.json`을 건드리지 않았다. 코드 쪽 불안정 영역은 직전 세션 기재가 그대로 유효

---
## [캠코 회의 실시 · 오픈 API 형식 버그 발견·수정 · 제품 방향 재정리] 2026-09-07 18:05 KST

### 완료
- **사전 공유 자료 발송** (2026-09-06 17:35, 동원님) — `04`를 임대=비타민스테이션으로 통일하고 PDF 생성(`04_캠코_사전공유자료_20260907.pdf`, A4 3쪽). 6절을 임시 접속 주소 안내로 교체. 금지표현 0건. ngrok 임시 URL(`overripe-astrology-impure.ngrok-free.dev`)을 본문에 포함
- **캠코 화상회의 실시** (2026-09-07 16:00, Zoom, 약 30분) — 정인기 차장. 회의록 `11_캠코_회의록_20260907.md`에 STT 전문 보존 + 정리
  - 사전 자료 5절 확인 요청 3건 **전부 답을 받음** — 표기는 온비드 값 그대로 / 가격=최저입찰가격·보증금=이용기관이 정한 비율(매각·임대 동일 구조) / 최저입찰가격 없으면 온비드가 「비공개」로 표시하며 공개 여부를 이용기관이 선택
  - 새 사실 — 오픈 API 28종, 화면 검색과 API가 같은 SQL 쿼리, 최대 30분 지연, 차세대 온비드 2026-04 오픈(6개월 미만, 보완 중)
  - 🔴 **협업 가능성** — "개발이 완료되거나 아이템이 확정되고 도움이 되겠다고 보이면 붙여서 서비스해 줄 수 있다". 선례는 예상 낙찰가·등기부등본(민간 서비스를 온비드가 당겨 씀)
  - 🔴 **입찰자 입장의 서류 체크 로직은 온비드에 없음**. 기관마다 서류·양식이 다양해 규칙으로 잡기 어렵다는 이유. "참고용 체크리스트는 괜찮은 아이디어" 평가. **단 밸리데이션으로 만들어 진행을 막으면 안 된다는 조건**
- **Zoom 자동 요약 대조** — 회의록 3-2절. 「문서 검증」·「유효성 검사」·「결제 상태 추적」 등 뜻이 뒤바뀐 3건, 고유명사 오기 7건(지지옥션→Auction.com 등), 누락 7건(협업 발언 전체 포함) 확인. **Zoom 요약은 사실 인용에 쓰지 않음**
- 🔴 **[코드] 오픈 API 물건관리번호 형식 버그 발견·수정** — 온비드 페이지 hidden input은 `20260800045167`(하이픈 없음), API는 `2026-0800-045167`만 인정. 하이픈 없으면 오류가 아니라 200 + `NODATA_ERROR`로 와서 7월 4일부터 두 달간 원인 미상이었음
  - `src/server.py`에 `api_cltr_mng_no()` 추가(4-4-6 하이픈 변환), `fetch_public_data_bundle` 진입부에 배선
  - 검증 3분할 — 순수 함수 9/9 통과(하이픈 미삽입 변이로 6/9 붉어짐 확인) / 배선: `missing_key`→`connected`, 호출 4종 200 / **화면 값은 키 유무와 무관하게 동일(회귀 0)**. API가 각 항목의 마지막 대안이라 크롤링이 값을 찾으면 API 차례가 오지 않음
- **API 상태 실측** — 6종 전부 승인(신청일 2026-07-04, 만료 2028-07-04, 개발계정). 재산유형코드 커버리지: 압류재산 54,642 / 기타일반재산 16,842 / 국유재산 3,636 / 공유재산 356 / 수탁재산 37. 물건상세 1건에 84필드 중 66필드 값 존재
- **신고 메일 발송** (2026-09-07 17:44, 메시지 `1a07b0a2872fcacd`) — 형식 불일치 재현 정보 + 트래픽 1,000 대 100,000 문의. 초안 `14_캠코_API형식_신고메일_초안_20260907.md`
- **문서 신규 5건** — `09_미팅노트`(질문 구성 5차 개편) · `11_회의록` · `12_제품방향` · `13_경매업체_서비스분석`(subagent) · `14_신고메일초안`. `08_진행시나리오`는 09로 대체 표시
- **제품 방향 확정** — 핵심은 **첨부파일에서 준비서류를 뽑아 체크리스트로 만드는 것** 하나. 물건 검색·지도·예상낙찰가·시세는 포화라 따라가지 않음. UI/UX는 적당히 편한 수준까지만

### 미완료
- 🔴 **박선호 차장 참조 누락 2회** — 9/2 연기 메일, 9/7 신고 메일 모두 `cc` 없음. 캠코 회신이 단독 발신이라 전체 회신만으로는 안 들어감. 다음 발송 시 손으로 추가
- 🔴 **미커밋 8건** — 추적 문서 4개는 커밋 `21259d3`(2026-09-07 17:48)로 푸시했다. 남은 것은 아래다
  - `src/server.py` — 물건관리번호 형식 변환. **검증은 끝났고 커밋만 안 됐다**
  - `04_캠코_사전공유자료_초안_20260902.md` 수정과 `04_캠코_사전공유자료_20260907.pdf`
  - `08_미팅_진행시나리오` · `09_미팅노트` · `11_회의록` · `12_제품방향` · `13_경매업체_서비스분석` · `14_신고메일초안`
- **API 66개 필드가 화면에 미반영** — 특히 최저입찰가격 「비공개」(`lowstBidPrcIndctCont`)를 0으로 표시하는 문제가 그대로. 감정평가금액·임대방식·임대기간·명도책임·공동입찰 가능 여부 등도 미사용
- **준비서류 추출 모듈 미착수** — 표본 수집과 정답 세트 작성이 선행. 규칙 기반이냐 모델이냐는 표본을 보기 전에 정하지 않기로 함
- 발표 슬라이드 제작 · 원고 낭독 실측 · 상시 접속 URL 결정 · 원격 레포 생성 (기존 미완료 유지)
- 인증키가 이 세션 기록에 노출됨 — 재발급 검토 필요

### 다음 세션 시작점
- 미커밋 변경 정리 후 커밋
- 첨부파일이 있는 공고 10건 이상 수집 → 사람이 정답 서류 목록 작성 → 추출 방식 비교. `12_제품방향_20260907.md` 6절 순서대로
- 최저입찰가격 「비공개」를 화면에 반영(작고 효과 큰 수정)

### 불안정 영역
- **저는 이번 세션에서 `docs/90_MVP개발/`을 읽지 않고 판단해 3회 틀렸습니다.** 인증키 신청 필요·API 커버리지·크롤링 선택 이유 전부 그 폴더에 답이 있었습니다. 설계 의도·과거 실측을 물으면 그 폴더를 먼저 읽으십시오
- `apis.data.go.kr`가 2026-09-06 저녁에 TLS 무응답이었다가 9/7 오전 정상. 원인 미규명. TCP는 열리는데 TLS/HTTP만 막히는 형태였음
- 경매업체 분석(`13`)의 모든 「없음」 판정은 **비로그인 화면 기준**. 로그인·결제 이후는 4곳 모두 미확인
- 🔴 **`awk` 의 `length` 는 문자가 아니라 바이트를 센다** — macOS 실측: `printf '가나다' | awk '{print length}'` → `9`. 한글 문서에서 줄 길이가 약 3배로 부풀려진다
  - 이 때문에 조사자의 「100자 초과 0건」 보고를 부정확이라 판정했으나 **제 판정이 틀렸다.** 문자 기준 재측정 결과 0줄, 최대 91자다
  - 줄 길이를 셀 때는 `python3` 의 `len()` 을 쓴다. 전역 지침의 `awk 'length > 400'` 도 같은 문제를 가진다(사용자 판단 사항)

---
## [내일 캠코 미팅 진행 시나리오 준비 · 04↔05 임대건 불일치 발견] 2026-09-06 15:59 KST

### 완료
- **`git pull`** — 원격과 동일(Already up to date)
- **캠코 마지막 메일 재확인** — 스레드 `1a0565f2ff2fba43`, 정인기 차장 2026-09-02 회신 "넵 9월 7일 16:00에 하시지요"로 **내일(9/7 월) 16:00 Zoom** 확정. 서면 문의는 트래픽(6종 일 10만)·화면표시형 이용 제약 없음으로 종결, URL 변경 이슈만 "회의때 자세히"로 남음
- **미팅 진행 시나리오 작성**(대화로 전달) — 16:00~17:00을 오프닝2·시연10·논의20·자유23·마무리5분으로 배분. 시연은 `05` 예행본(임대 비타민→준비보드→탭복귀→매각 부산, 가격0→질문3 연결) 기준. 아직 파일 미저장(동원님 확인 후 `07_미팅_진행시나리오.md` 저장 예정)
- **정합성 이슈 발견** — 실파일 `04_사전공유자료` 4절 임대 시연 공고 = **마포 상암동**인데 `05_시연순서`·to-do·today 기재 = **비타민스테이션**. 발송 전 임대 건을 하나로 통일해야 자료와 시연이 어긋나지 않음

### 미완료
- 🔴 **Zoom 링크 개설 + 사전 자료 메일 발송**(박선호 차장 cc) — 여전히 미발송. 전날(9/6=오늘)까지 약속. 동원님 직접 액션
- **임대 시연 건 통일 결정**(비타민 vs 마포) — 동원님께 질의했으나 세션 종료 시점 미응답
- 미팅 시나리오 파일 저장(위 결정 후) · 발표 슬라이드 · 낭독 실측 · 외부 접속 URL 결정(기존 미완료 유지)

### 다음 세션 시작점
- 임대 건 통일 → `04` 정합화 → Zoom 링크·사전자료 발송(오늘 중 또는 내일 오전, **박선호 차장 cc**)
- **회의 당일(9/7) 오전 시연 URL 3건 재검증** — 온비드 URL 수명 짧음
- 시나리오를 `docs/60_본선준비/07_미팅_진행시나리오.md`로 저장

### 불안정 영역
- 추적 문서(`to-do.md` P0-4 · `today.md` D260906-02)는 04를 "비타민으로 교체 완료"로 기재하나 **실파일 04는 마포**다. 어느 쪽이 맞는지는 동원님 결정 전까지 미확정 — 이번 세션은 실파일을 수정하지 않고 불일치만 기록
- 코드·문서 내용 변경 0건 세션이라 `changelog.md` 미갱신(기재 기준 미해당)

---
## [시연 URL 재확보 · long-auto-loop(마감경고·e2e)] 2026-09-06 04:40 KST

### 완료
- **9/7 회의용 시연 URL 3건 재확보·검증** — 매각 1(부산 금정구 서동 교육연구시설, 소해원, 마감 2027-05-12)·임대 2(서울 서초 비타민스테이션 예술의전당 마감 2026-10-01 / 서울 마포 상암동 서울경제진흥원 마감 2026-09-30). 3건 모두 마감이 회의 시각(9/7 16:00) 이후. 로컬 서버 `/api/analyze` 실호출로 유형·기간·기관·AI(connected)·준비보드 5건 검증. 5개 파라미터는 물건상세 hidden input에서 직접 추출(공유하기 클립보드 복사 불필요). 커밋 `325caaf`
- **문서 정합화** — `03_시연용_실URL`(재확보분)·`04_사전공유자료`(시연 공고 2건 교체)·`05_시연_실행순서`(임대=비타민 price0→질문3 흐름 보존, 매각=부산 금정구)·`10_발표자료 피치`(실증 결과 재측정: 임대·매각 각 확인필요 3건, 소요 1.9~2.0초). 전부 금지표현 0건. 커밋 `c83b107`·`88f0b1f`·`29f3b49`·`8cf4eff`
- **[코드] 마감된 공고 '입찰 마감 지남' 경고 구현** (동원님 승인) — `src/server.py`에 `bid_deadline_passed()` 추가. 입찰기간 종료값/마감일시를 분석 시각과 순수 날짜 비교, 과거면 alerts 맨 위에 사실 경고 prepend. 입찰 여부·법률·수익성 판단 없음(가드레일 준수). helper 단위 7종 + 서버 실호출 2건(마감건 경고 O·진행건 X). 커밋 `6d74d9f`
- **[코드] e2e 단언 키 없는 환경용 조건부 수정** (동원님 승인) — API 상태 단언을 `#report-hero`→`#source-notes` "보조 데이터:"로 이동, 연결 상태는 `ONBID_API_SERVICE_KEY` 있을 때만 단언. 이 기기에 `npm install`+`playwright install chromium` 신규 수행 후 로컬 러너로 **1 passed (3.4s)** 확인. 커밋 `1c537bb`
- `docs/auto-report-20260906-01.md` 작성(7개 이터레이션 요약)

### 미완료
- 🔴 **Zoom 링크 개설 + 사전 자료 메일 발송**(박선호 차장 cc) — 외부 액션, 동원님 직접
- 외부 접속 URL 제공 여부 / 원격 레포 생성 / supporting-programs 정리 — 사용자 결정
- 발표 슬라이드 제작(포맷 결정 필요) · 원고 낭독 시간 실측(사람 낭독 필요)
- e2e **키 있는 경로 Not Verified** — `ONBID_API_SERVICE_KEY` 미보유. "보조 데이터 조회:" 단언은 키 있는 환경에서만 실행
- 마감 경고 **UI 화면 캡처 미보관** — API·MCP 브라우저로 데이터는 확인, 렌더 경로는 기존 그대로

### 다음 세션 시작점
- **회의 당일(9/7) 오전 시연 URL 3건 재검증** (온비드 URL 수명 짧음) → Zoom 링크·사전 자료 발송
- 슬라이드 포맷을 정하면 제작 착수 가능

### 불안정 영역
- 시연 URL은 회의 한 번 밀리면 무효가 될 만큼 수명이 짧다. 페이지 번호가 아니라 조건검색으로 그때그때 재확보할 것
- 이 기기에 `node_modules`·Playwright chromium이 이번에 설치됨(이전엔 macOS만). `package-lock.json`은 EOL 워킹카피 차이만 있어 미커밋 상태

---
## [회의 일시 확정 전파 · today 작성] 2026-09-03 14:49 KST

### 완료
- **캠코 화상회의 일시 확정 확인** — 정인기 차장 회신 2026-09-03 08:55 KST 수신, 메시지 `1a0648cce0179f73`, 본문 "넵 9월 7일 16:00에 하시지요."
  - 확정 일시는 **2026-09-07(월) 16:00**이고, 9/2 연기로 취소된 9/3 16:00을 대체한다
  - 전 세션 handoff의 "새 일시 회신 대기"는 이 회신으로 해소됐다
- **낡은 일시 기재 전파 갱신** — 8개 파일 13건. 커밋 `9a9744d`
  - `to-do.md` P0-4: 일시 확정 대기 항목을 완료로 닫고, Zoom 링크 발송 기한을 9/6으로 명시
  - `to-do.md` P0-3: 시연 URL 재확보 조건을 9/7 16:00 기준으로 교체
  - `handoff.md` 전 세션 블록: 미완료·다음 시작점 2건에 확정 표기
  - `00_본선준비_계획`: "2026-09-03 진행 상황" 블록 추가
  - `02`·`03`·`04`·`05` 본선준비 문서: 일시·만료 판정 5건 교체
  - 종료 조건 실측 — `grep '일시 미확정|일정 확정 대기'` 잔존 0건
- **연기 요청 메일의 참조 누락을 원문 헤더로 확증** — 메시지 `1a0612e210904ec5`의 `to`는 정인기 차장 1명이고 cc가 없다
  - 본문에 보이는 "참조: sunho327"은 인용된 9/1 메일의 헤더이지 이 메일의 것이 아니다
  - 다음 발송에 박선호 차장을 포함하라는 경고를 `to-do.md`에 남겼다
- `docs/today.md` 재작성 — `D260903-01`~`-08` 8건, 24줄·2,629바이트. 400자 초과 줄 0건

### 미완료
- 🔴 **시연 URL 3건 재확보** — 확보한 3건은 모두 9/7 16:00 회의에 쓸 수 없다. 김해(09-07 10:00)·제주(09-04 10:00)는 회의 전 마감이고 원인재역(09-07 16:00)은 회의 시작 시각에 마감된다
- 🔴 **Zoom 링크 개설·사전 자료 발송** — 9/6까지. 동원님이 직접 처리하기로 한 항목이라 착수하지 않았다
- 🔴 **외부 접속 URL 제공 여부 결정** — 발송의 선행 결정인데 이번 세션에서 다루지 않았다
- 사전 공유 자료의 **시연 공고 2건 교체** — URL 재확보가 선행이라 일시만 고치고 공고는 그대로 두었다
- `D260903-05`~`-08`(마감 경고 방침·슬라이드·낭독 실측·e2e 방침) 전부 미착수
- 코드 변경 0건이라 `changelog.md`는 갱신하지 않았다 — 이 파일의 기재 기준(마일스톤·기능 구현·근본 수정)에 해당하지 않는다

### 다음 세션 시작점
- `docs/today.md`의 `D260903-04`(시연 URL 3건 재확보)부터 — 사전 자료 발송의 선행 조건이다
- 확보 후 `04_캠코_사전공유자료_초안_20260902.md` 3절의 시연 공고를 교체한다
- 그 다음 `D260903-02`(외부 접속 URL 제공 여부)를 동원님과 결정한다

### 불안정 영역
- 이번 세션은 문서만 고쳤고 `src/`는 건드리지 않았다. 코드 쪽 불안정 영역은 전 세션 기재가 그대로 유효하다
- 시연 URL의 마감 시각은 **회의 시작 시각과 비교**해야 한다. 날짜만 보면 원인재역처럼 회의 중 마감되는 건을 걸러내지 못한다
