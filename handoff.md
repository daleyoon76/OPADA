# Handoff — Hometest_op

(각 세션 종료 시 아래 형식으로 항목 추가. 최대 10개 유지, 초과 시 가장 오래된 항목 삭제)

---
## [구현] 첨부추출 프로토타입 서류명 어휘를 production 배선에 병합 2026-09-20 21:42 KST

### 완료
- **9/18 방치된 프로토타입(`src/attachment_extractors/submission_docs.py`)의 서류명 어휘를 production(`src/server.py`)에 병합했다** — 커밋 `a3a9024`
  - 두 `DOC_NAME_PATTERNS` 목록을 정규식으로 직접 대조해 **새 항목 17개**를 확인 — 청렴이행각서·청렴계약서·사업제안서·제안서 접수조서·임차신청서·제소전화해 신청동의서·시설물설치 이용승낙신청서·재직증명서·건강보험자격득실확인서·관광사업등록증·등급인정서·평가결과통보서·부가가치세 과세표준증명서·표준재무제표증명서·공유재산 사용허가 신청서·성인지 관점 고려 확인서·정보통신망 이용 송달 동의서
  - 기존 항목 2개의 패턴도 프로토타입 수준으로 넓혔다 — 컨소시엄 협정서(`공동수급 표준협정서` 대안 추가) · 국세 및 지방세 완납증명서(구분자 사이 삽입어 허용)
  - 순서는 프로토타입이 이미 지키던 「긴 이름 우선·연관 항목 인접 배치」 관례를 그대로 따랐다(직접 비교로 검증, 짐작하지 않음)
- **절 앵커+윈도(제출서류/구비서류 절 근처 800자만 탐색) 구조 변경은 실측 후 도입하지 않기로 결정했다**
  - 10건 극단 사례(`attachment_answer_key.json`)에서는 정밀도가 오른다(62%→76%)
  - 그런데 **176항목 정답 세트에서는 재현율이 떨어지고(68%→58%) 단계 판정 오류가 2배로 늘어난다(25→52)**
  - 원인으로 보이는 것 — 윈도가 임의 글자 수에서 잘라 줄 경계를 끊고, `carried_stage`(계약단계 물림) 로직이 줄 단위 연속성에 의존하기 때문
  - 두 세트 다 좋아지는 것이 아니라 트레이드오프이고, 서비스가 실제로 커버해야 하는 범위(176항목)에서 악화이므로 미도입
- **`tests/measure_attachment_extraction.py`에 production 배선 측정 경로를 추가했다** — 프로토타입(`extract_submission_docs`)과 실제 서비스(`server.extract_doc_items`)를 나란히 잰다. 이 스크립트가 지금까지 프로토타입만 재고 있었다
- **측정 수치** `[관측]`
  - 176항목 세트(`tests/measure_answer_set.py`) 배선 경로 — 반영 전 104/176=59% → 반영 후 **119/176=68%**. 헛뽑음 8(함정1) → 10(함정2). 단계 불일치 24 → 25
  - 10건 세트(`attachment_answer_key.json`) production 경로 — 반영 전 정밀도64%/재현율49%(TP61 FP35 FN64) → 반영 후 **정밀도62%/재현율58%**(TP72 FP44 FN53)
  - 게이트 3종 재실행 — 순수 함수 **251/251**(축 28개 미실행 0) · 화면 문자열 **20/20** · e2e(playwright) **17/17**. 전부 통과
- **라이브 검증 2건** — 서버(8975) 재시작(자동 재기동 감지) 후 실제 온비드 URL로 `/api/analyze` 호출
  - idx21(김해공항 주차대행서비스, 2026-09-17 템플릿 표본) — 첨부 서류 **14건 → 20건**(사업제안서·임차신청서·재직증명서·제소전화해 신청동의서·시설물설치 이용승낙신청서·청렴계약서 추가). 브라우저로 화면 렌더까지 직접 확인(스크린샷)
  - idx23(인천공항 T2 환승호텔 변경공고) — **9건 → 16건**(청렴계약서·재직증명서·관광사업등록증·등급인정서·평가결과통보서·부가가치세 과세표준증명서·표준재무제표증명서·사업제안서 추가)

### 미완료 / 미검증
- **push 안 함** — 커밋 `a3a9024` 로컬에만 존재(지시에 따름)
- **완전히 새로운 서류명은 여전히 못 잡는다** — idx22(전기공사류 첨부)의 `간선계산서`·`부하계산서`·`스프링클러범위도`·`처리필증` 등은 닫힌 어휘 밖이라 이번 병합으로도 못 뽑힌다. 규칙기반 방식(닫힌 어휘 매칭)의 구조적 한계이며, 이번 작업 범위(추출 정확도 개선)로는 못 넘는다
- **헛뽑음이 10건 세트에서 늘었다**(FP 35→44) — 열린 정규식(`[가-힣]{2,10}\s*등록증`/`면허증`)이 신규 어휘와 함께 더 많은 변형을 잡으면서 「운전면허증」·「주민등록증」류 오탐도 함께 늘었다. 재현율 개선(+9pp)과 맞바꾼 트레이드오프이며 이번 작업 범위 밖(물건 명세·자격요건 등)은 손대지 않았다

### 다음 시작점
- 헛뽑음 증가(idx14·21·22·23)를 줄이려면 열린 등록증/면허증 정규식에 부정 목록(음식점허가증 등 서류 아닌 것 배제)을 검토할 수 있다 — 이번 세션 범위 밖으로 미룸
- 완전히 새로운 서류명 대응은 규칙기반의 한계이므로, 모델 기반 보조 추출(9/17 템플릿 문서 §6 권고)을 다음 단계로 검토

### 불안정 영역
- 서버(8975)가 자동 재기동 감지 체계(파일 변경 감시로 추정)로 붙어 있다 — `kill` 직후 다른 PID 로 즉시 재기동됨을 실측했다. 재기동 메커니즘 자체는 이번 세션이 조사하지 않았다
- 절 앵커+윈도 실험은 `/tmp` 임시 스크립트로만 했고 레포에 남기지 않았다 — 재현하려면 이 handoff 항목의 수치와 `tests/measure_answer_set.py`·`tests/measure_attachment_extraction.py`의 기존 로직을 참고해 다시 구성해야 한다
---
## [테스트 · 발견] 사용자 여정 재현 가이드 신설 + 실공고 9건 라이브 점검 2026-09-20 13:10 KST

### 완료
- **재현 가이드 신설** `docs/80_사용자여정테스트/00_사용자여정_재현가이드_20260920.md` — 삼년 플레이북 §0 원칙만 가져오고 OPADA 화면 기준으로 새로 작성. 사용자 유형×검토 목적 매트릭스를 코드 근거로 4개 조합으로 추림
- **실공고 9건 라이브 분석** — 물건상세 3건(부산 금정구 매각·비타민스테이션 임대·상암동 임대) + 공고상세 6건(idx1 압류재산·idx4 국유재산 대부·idx21 김해공항 주차대행 등). 결과 `docs/80_사용자여정테스트/results/2026-09-20-journey-결과.md`
- **아코디언(9/18 신설, 법인/개인 조건) 실공고 검증** — 지난 회차에 빠져 있던 항목. idx1(표본 23건 중 "법인" 그룹을 가진 유일한 공고)로 법인↔개인 전환 시 펼침/접힘이 코드와 일치함을 확인. "개인" 단독 그룹을 가진 실공고는 표본에 0건 — 다음 회차 과제로 남김
- **공고상세 URL 직접 입력 500 재현** — idx1의 물건상세 링크가 안내대로 실제 HTTP 500(curl 실측). 알려진 정상 동작과 일치
- **게이트 3종 재실행** `[관측]` — 순수 함수 251/251·축 28개 미실행 0 · 화면 문자열 20/20 · e2e(playwright) 17/17. 코드 수정 없어 전후 비교는 없음
- **콘솔 에러 0건** — 데스크톱·모바일(390px) 전 구간
- `to-do.md`·`known-pitfalls.md`에 신규 발견 3건 등재(아래 미완료 참고)
- **작업 체크 상태가 공고별로 구분되지 않는 결함을 닫았다** — 공고별 저장 키 분리(커밋 `19c56a9`) + 초기화 버튼이 관심 공고 목록 배지를 갱신하지 않는 결함 수정(커밋 `b209daf`, `resetTaskState()`에 `saveRecentNotice()` 추가). 서로 다른 공고 2건으로 라이브 재검증 — 체크·초기화·새로고침 전부 배지가 일치, 콘솔 에러 0건. 게이트 3종 재실행 통과(순수 함수 251/251·화면 20/20·e2e 17/17)

### 미완료 / 미검증 — 제안만 남기고 구현하지 않음(화면 구조·제품 판단 필요)
- 🟡 **인쇄용 화면이 최초 커밋부터 한 번도 구현된 적이 없다** — `.print-sheet`/`#view-print` CSS만 있고 트리거·렌더링 요소가 없다(`git log -S` 확인)
- 🟡 **회차(여러 입찰 차수) 공고에서 입찰기간·최저입찰가격을 못 읽는다** — 압류재산·국유재산 대부입찰에서 재현(idx1·idx4). `values_after_label()`이 표 구조를 인식 못함. `known-pitfalls.md` 등재
- API 실호출 경로는 이 기기에 `ONBID_API_SERVICE_KEY`가 없어 여전히 미검증(기존과 동일)

### 다음 시작점
- 위 3건 중 어느 것을 먼저 고칠지 동원님 결정 → 관심 공고 진행률 버그가 사용자 체감 영향이 가장 큼
- "개인" 조건 단독 그룹을 가진 실공고 확보(아코디언 완전 검증)

### 불안정 영역
- 이번 세션은 코드를 수정하지 않았다 — 위 3건은 관측·원인 분석까지만이고 수정 범위·방향은 미결정
- idx1·idx4처럼 마감이 지난 공고도 온비드 원문 열람 자체는 계속 가능했다(페이지가 사라지지 않음). 다만 회차 파싱 결함은 마감 여부와 무관하게 재현된다(진행 중인 회차가 있어도 같은 표 구조라 동일하게 실패)

---
## [인프라 · 기록] ngrok 고정 도메인 상시화 · 운영계정 보류 · 회의 대조표 2026-09-08 22:27 KST

### 완료
- **공개 접속 주소를 상시화했다** — `https://overripe-astrology-impure.ngrok-free.dev`. 커밋 `419598c`·`c71a8e7`
  - LaunchAgent 2개(`com.dale.hometest-server`·`com.dale.hometest-ngrok`) · `RunAtLoad` · `KeepAlive`
  - **세션과 무관하게 산다.** 이 세션이 끝나도 유지된다
  - 🔴 **TCC 관문** — `~/Documents` 는 보호 폴더라 LaunchAgent 가 `Errno 1` 로 죽었다. 전체 디스크 접근 권한으로 해소
    - 대상은 `/Library/Developer/CommandLineTools/usr/bin/python3` 다. **`/usr/bin/python3` 은 별도 스텁이라 통하지 않았다** `[관측]`
  - **「재기동마다 주소가 바뀐다」는 틀린 기재였다** — authtoken 이 없어 임시 주소만 받던 상태를 요금제 제약으로 오해했다
- **내성·도달 검증** `[관측]`
  - 서버 `kill -9` → PID 55139→55365 자동 재시작 · ngrok `kill -9` → 47396→55687 **주소 동일**
  - 공개 주소 정적 파일 HTTP 200 · 로컬 원본과 sha256 동일(`17c344f2…` 7,799B)
  - 공개 주소 `/api/analyze` **HTTP 200 · 0.56초 · `ok=true`** (마포구 상암동 업무시설)
  - `index.html` 수정이 **서버 재기동 없이** ngrok 경유로 반영됨 — `translate_path` 가 요청마다 디스크를 읽는다
- **운영계정 신청 — 폼까지 진입해 절차를 실물 확인하고 보류로 닫았다.** 커밋 `d1d2294`
  - 경로 `마이페이지 > 활용신청 현황 > [승인] API > 운영계정 신청하기` → `selectRunAcountRequestForm.do`
  - 신청 단위는 **API별**(6종이면 6회) · 활용사례 등록은 **폼 안에 통합** · 필수 입력 11개
  - 블로커는 **서비스 URL** 하나였다. ngrok 고정 도메인이 생겨 **이제 풀렸다**
  - **제출하지 않았다** — 전후 스냅샷 동일(전체 11건 · 활용신청 0) `[관측]`
- **2026-09-07 캠코 회의 발언 대조표 M1~M15 신설.** 커밋 `f492d87`
  - ✅ 반영 9 · 🔶 부분 2 · ⬜ 미착수 2 · 🔴 미반영 1 · 후속 없음 1
  - 🔴 **M14 최대 30분 지연 안내가 화면에 없다** — `src/` 전수 grep 0건. 값이 항상 최신인 것처럼 보인다

### 미완료 / 미검증
- 🔴 **ngrok authtoken 이 트랜스크립트에 노출됐다** — `~/.claude/projects/` 1건 · `history.jsonl` 1건. **저장소 안은 0건**
  - claude-sync 해제 상태라 외부 유출 경로는 없다(심링크 0 · `~/.claude.json` 실파일) `[관측]`
  - **회전 권장.** 대시보드 `Regenerate` 후 `read -s` 로 입력한다. 고정 도메인은 계정 귀속이라 유지된다
- 🔴 **공개 서버에 인증이 없다** — 주소를 아는 사람은 누구나 분석을 돌린다
  - 지금은 이 기기에 `ONBID_API_SERVICE_KEY` 가 없어 실질 피해가 없다
  - 키를 넣고 공개해 두면 **남이 우리 일 1,000건 한도를 쓴다.** 접근 제한이 함께 필요하다
- **마이페이지 상세에 일반 인증키가 발급돼 있는 것을 확인했다** — 값은 어디에도 기록하지 않았다
  - 넣을지는 미결정이다. `handoff.md` 과거 블록에 「넣지 않기로 결정됨」이 있어 임의로 진행하지 않았다
- **launchd 는 사용자 로그인 세션 기준이다** — 로그아웃 상태에서의 동작은 확인하지 않았다 `[미검증]`

### 다음 시작점
- 🔴 **M14** — 「온비드 원문 기준, 최대 30분 지연될 수 있습니다」 한 줄. 문구 1줄이고 마감 직전 오해 비용이 가장 크다
- authtoken 회전 → 그 뒤 인증키 투입 여부와 접근 제한을 한 묶음으로 결정
- 운영계정 신청 재개 판단 — 확정값은 to-do 에 있다(1인 창업 기업 · 경기도 · 재정금융 · 웹사이트)

### 불안정 영역
- ngrok 무료 요금제는 브라우저 접근에 **안내 페이지를 한 번 끼운다.** 외부인에게 줄 때 함께 안내해야 한다
- 공개 주소는 맥미니가 켜져 있고 로그인돼 있을 때만 산다. 상시성의 상한이다

---
## [게이트 신설 · 수정] API 경로 3건 — C1 회귀 축 · 오류 봉투 · https 2026-09-08 17:15 KST

### 완료
- **[①] `api_cltr_mng_no()` 회귀 축 C1 신설** — 커밋 `277ec5e`. 기존 게이트 `tests/test_docs_extraction.py` 에 축을 얹었다
  - **이 함수는 두 달짜리 결함을 막는 자리인데 레포에 회귀 테스트가 0건이었다.** 통째로 지워도 게이트가 초록이었다
  - to-do 의 「순수 함수 9/9 통과」(2026-09-07)는 **커밋되지 않은 임시 게이트**였다. 지금 게이트에 편입했다
  - 축 4갈래 — 하이픈 없는 14자리 · 구분자 섞인 14자리 · 멱등(`f(f(x)) == f(x)`) · 14자리 아님(원문 통과) · 빈값/None
  - 붉히면 안 되는 입력 4개 — `pbctCdtnNo` 류 숫자 · `pbancMngNo` 6-5-2 · 13자리/15자리 · 원문 통과에 하이픈 미생성
  - 배선 축을 따로 뒀다 — `fetch_public_data_bundle` 이 만드는 **실제 요청 URL** 에 `cltrMngNo=2026-0800-045167` 이 실리는지 본다
- **[②] `api_header()` 게이트웨이 오류 봉투 분기** — 커밋 `c2a66dd`. `gateway_error_header()` 신설
  - `OpenAPI_ServiceResponse.cmmMsgHeader.returnReasonCode` → `resultCode`, `returnAuthMsg`(없으면 `errMsg`) → `resultMsg`
  - 기존 세 형태(`response.header`·`result`·`header`)보다 **뒤에** 뒀다. 정상 경로 불변을 음성 대조 6건으로 고정
  - `api_items()` 에 같은 봉투 가드를 함께 넣었다 — 없으면 마지막 분기가 **오류 봉투를 item 1건으로** 돌려준다
- **[③] `PUBLIC_DATA_ENDPOINTS` 6종 스킴 `http` → `https`** — 커밋 `e87d74b`. 호스트·경로는 그대로, 스킴만 바꿨다
- **게이트 실측 `153/153 · 축 21` → `203/203 · 축 24` · 연기 3 → 5** `[관측]`
  - 연기가 는 것은 `urlopen` 과 `public_data_service_key` 를 배선 축에서 연기하기 때문이다. 머리에 그대로 찍힌다
- **킬러 변이 7개 전부 `rc=1`** `[관측]` · 변이 없는 사본은 `rc=0`(음성 대조). 러너 `/tmp/opada-mutate.sh`
  - M1 하이픈 삽입 삭제(8건) · M2 자리수 4-6-4(6건) · M3 슬라이스 소스 `digits`→`value`(3건) · M4 배선 삭제(2건)
  - M5 게이트웨이 분기 삭제(6건) · M6 `returnReasonCode` 미이관(3건) · M7 한 줄만 `http` 되돌리기(3건)
  - 변이마다 `~/Library/Caches/com.apple.python/` 를 지우고 `grep` 으로 반영을 확인한 뒤 돌렸다

### 자작 종료 조건 (내가 만진 델타)
- **최종 커밋에서 변이 재실행** — 커밋 3건을 넣은 뒤 같은 러너를 한 번 더 돌렸다 → 7/7 `rc=1` 유지 `[관측]`
- **`grep -c "http://apis" src/server.py` → 0** `[관측]`. 상수만 보는 것이 아니라 배선 축이 6종 요청 URL 을 실제로 잰다

### 미완료 / 미검증
- 🔴 **③ 라이브 미검증** — 이 기기에 `ONBID_API_SERVICE_KEY` 가 없다. `https` 실호출은 하지 않았다
  - 구현됨 / 호출부 배선 검증됨 / 라이브 미검증 3분할이다. 실호출은 키가 있는 자리에서 한 번 확인해야 한다
- 🔴 **② 라이브 미검증** — 실제 코드 22(한도 초과) 응답을 받아 보지 못했다. 봉투 구조의 출처는 2026-09-08 더미 키 응답 본문 관측이다
- **`notice_list`·`real_estate_list` 필수 파라미터는 손대지 않았다** — 호출부가 없어 지금 넣으면 죽은 코드다. to-do 유지
- **화면 게이트 미실행** — `tests/screen.spec.js` 는 이번에 돌리지 않았다. 이번 diff 는 API 경로뿐이라 화면 축과 겹치지 않는다

### 다음 시작점
- 키가 있는 자리에서 ②③ 라이브 1회 — `https` 실호출 200 확인 · 한도 초과 응답이 오면 `resultCode 22` 가 화면에 뜨는지
- `api_cltr_mng_no()` 는 14자리가 아니면 **조용히** 원문을 통과시킨다. C1 이 그 동작을 «고정»했을 뿐 «고친» 것은 아니다

### 불안정 영역
- C2 배선 축은 `urlopen` 을 연기한다. 게이트웨이가 **실제로** 그 봉투를 그 코드로 주는지는 이 게이트가 못 잰다
- `api_items()` 가드는 `OpenAPI_ServiceResponse` 키 하나만 본다. 다른 이름의 오류 봉투가 오면 여전히 봉투가 item 이 된다

---
---
## [대조] 온비드 API 참고문서 26개 ↔ 코드 요청 파라미터 규격 2026-09-08 16:50 KST

### 완료
- **상위 세션이 확보한 참고문서 docx 26개 + `index.tsv` 를 근거로 6종 요청 파라미터를 대조했다** `[관측]`
  - 보관 `~/Downloads/onbid-api-guides/` (레포 미커밋). 추출은 `zipfile` → `word/document.xml`
  - 신규 기록 `docs/60_본선준비/16_온비드API_참고문서_규격대조_20260908.md`
- **요청 파라미터 형식 불일치는 추가로 없다.** 하이픈 건과 같은 부류의 새 결함 0건
  - `cltrMngNo` — VARCHAR(100) · 필수 · 샘플 `2024-1100-084555`(4-4-6). `api_cltr_mng_no()` 와 일치
  - `pbancMngNo` — VARCHAR(40) · 필수 · 샘플 `202406-21411-00`(6-5-2). 페이지가 주는 `202608-28020-01` 과 자릿수·하이픈 위치 동일. **변환하지 않는 것이 맞다**
  - `pbctCdtnNo` — **NUMBER(22) · 하이픈 없음** · 샘플 `3621804`. 우리 실측값 5건 전부 숫자 7자리. `cltrMngNo` 와 규칙이 다르다
  - `serviceKey`·`pageNo`·`numOfRows`·`resultType` 4종 공통 전부 규격 내
- **엔드포인트·오퍼레이션 6/6 일치** — 서비스 경로와 `getXxx2` 접미사가 참고문서 「Call Back URL」과 같다
- **트래픽** — `index.tsv` 실측 차세대 26종 **전부 개발계정 1,000**. 캠코 회신과 일치, 추가 정정 대상 0건
  - 「우리 한도가 100,000」 잔존 기재 **0건** (`grep -rn` 실행 결과)
- **직전 세션이 미완료로 남긴 「`pbancMngNo`·`pbctCdtnNo` 규격 미확인」을 닫았다**
- **정정 전파 4파일** — `known-pitfalls.md`(두 파라미터 규칙 신설)·`to-do.md`·`15_캠코_회신`·`08_오픈API28종조사`(부록 B)

### 미완료
- **다른 부류 지적 3건은 등재만 하고 고치지 않았다** — 이번 지시는 대조·보고였다
  - 🟡 스킴 — 규격은 6종 전부 `https://`, `src/server.py:25-30` 은 `http://`. 인증키가 평문으로 나간다
    - 동작 자체는 문제없다 `[관측]` — 더미 키로 http·https 둘 다 HTTP 403 · 212B · 동일 본문
  - 🟡 `api_header()`(`src/server.py:822`) 가 `OpenAPI_ServiceResponse.cmmMsgHeader` 를 못 읽는다. 개발계정 1,000건 초과(코드 22)가 HTTP 200 으로 오면 원인 없는 `failed` 로 보인다
  - 🔴 `notice_list`·`real_estate_list` 는 필수 파라미터가 코드에 없다(각 4개·2개). **호출부가 없어 지금은 무해**하고, 만들 때 함께 넣어야 한다
- **실호출 재검증 미실시** `[미검증]` — 이 기기에 `ONBID_API_SERVICE_KEY` 가 없다. 대조는 전부 정적이다
- **26개 중 20개는 열지 않았다** — 우리가 호출하지 않는 서비스. `08_…조사` 부록 B 에 신규 미검증 행으로 등재

### 다음 시작점
- `src/server.py:25-30` 스킴 6줄 `https` 교체 + `api_header()` 게이트웨이 봉투 분기 — 둘 다 작고 독립적이다
- `api_cltr_mng_no()` 회귀 축 신설(직전 세션에서 이월된 항목, 여전히 `tests/` 에 0건)

### 불안정 영역
- 대조는 **정적**이다. 「형식이 규격과 같다」는 실호출로 확인한 것이 아니다
- `api_cltr_mng_no()` 는 14자리가 아니면 원문을 조용히 통과시킨다(직전 세션에서 이월, 미해소)

---
## [기록 · 정정] 캠코 회신 반영 — 하이픈은 규격 · 트래픽은 계정 등급 차이 2026-09-08 16:40 KST

### 완료
- **캠코 정인기 차장 회신 수령 (2026-09-08 09:26 KST)** — 2026-09-07 17:44 신고 메일의 문의 2건에 전부 답을 받았다
  - 원문 기록 `docs/60_본선준비/15_캠코_회신_API형식_트래픽_20260908.md` **신설**
- **① 물건관리번호 하이픈은 버그가 아니라 API 규격이다** `[인용]`
  - "OpenAPI 서비스를 받으려면 하이픈을 붙여야 한다. 에러가 아니다. 참고문서에 샘플데이터가 명시되어 있다"
  - 우리가 「온비드 측 형식 결함」으로 신고한 건은 **신고 대상이 아니었다**. 규격 문서(`OpenAPI활용가이드_NN_….docx`)를 읽지 않은 것이 원인이다
  - 메일 본문의 요청 2번(「하이픈 없는 입력도 받아 달라」)은 **철회**로 기재했다
- **② 트래픽 100,000 은 운영계정 기준** `[인용]` — 개발계정은 1,000. 우리는 개발계정이다
  - 포털 실측(26종 전부 1,000)과 캠코 회신(100,000)의 불일치는 **계정 등급 차이**로 해소됐다. 미해결 항목 하나가 닫혔다
  - 운영계정 전환: 활용사례 등록 후 `마이페이지 > 오픈 API 활용 현황`의 API별 「운영계정 신청하기」
- **코드 실태 조사 — 6종 호출부 전부 확인. 하이픈을 떼는 곳은 없다. 코드 무수정** `[관측 · 정적]`
  - `src/server.py:900 api_cltr_mng_no()` 가 `cltrMngNo` 를 4-4-6 으로 **붙인다**(떼지 않는다). 하이픈이 이미 있어도 결과는 같다(멱등)
  - 호출부 4개(`real_estate_detail`·`item_bid_detail`·`notice_detail`·`notice_bid_detail`) 모두 `fetch_public_data_bundle` 진입부의 이 변환을 거친다
  - `notice_list`·`real_estate_list` 2종은 **정의만 있고 호출부가 없다** — 물건관리번호가 나가지 않는다
  - `pbancMngNo` 는 페이지가 이미 하이픈 포함(`202608-28020-01`)으로 준다. 원문 그대로 전달되며 훼손 지점 없음
  - `public_data_url()` 의 `urlencode` 는 하이픈을 unreserved 로 두어 인코딩하지 않는다
  - `app.js:594 noticeKey()` 는 공백만 `-` 로 바꾸는 **클라이언트 캐시 키**다. API 경로가 아니다
- **정정 전파 7개 파일** — `known-pitfalls.md`·`to-do.md`·`changelog.md`·`06_인증키_발급안내`·`11_회의록`·`14_신고메일초안`·`08_오픈API28종조사`

### 미완료
- **하이픈 유무 실호출 재관측 — 이 세션에서 못 했다** `[미검증]`
  - 이 기기에 `ONBID_API_SERVICE_KEY` 가 없다(env·shell profile·`.env` 전부 부재 확인)
  - 하이픈 유무로 결과가 갈리는 것은 **2026-09-07 다른 기기 실측**이 근거다(시연 3건 전부 NODATA↔데이터 1건). 이번 회신이 그 관측과 일치한다
- **`api_cltr_mng_no()` 회귀 테스트가 레포에 없다** — 2026-09-07 의 「순수 함수 9/9」는 커밋되지 않은 임시 게이트다. `tests/` grep 결과 0건
  - 지금은 이 함수를 지워도 게이트가 붉어지지 않는다. 두 달을 태운 결함인데 방지막이 없다
- **참고문서 미다운로드** — `pbancMngNo`·`pbctCdtnNo` 의 규격 형식은 여전히 **미확인**
- **운영계정 미신청**

### 다음 시작점
- 상위 세션(브라우저) — 참고문서 6종 다운로드 · 운영계정 신청. 이 세션 범위 밖으로 지정됨
- `api_cltr_mng_no()` 회귀 축을 `tests/test_docs_extraction.py` 에 신설(양성 1 + 하이픈 제거 변이 + 14자리 아닌 값 통과)

### 불안정 영역
- `api_cltr_mng_no()` 는 14자리가 아니면 **원문을 조용히 통과**시킨다. 변환 실패가 로그에 남지 않는다
- 개발계정 1,000건/일은 시연(공고 3건)에는 충분하나 **표본 23건 × 4종 = 92 호출**을 반복하면 근접할 수 있다

---
## [라이브 검증 · 임시 공개 배포] `/api/analyze` 실호출 공백 닫음 2026-09-08 08:55 KST

### 완료
- **체크리스트 재작성 이후 처음으로 라이브 실호출을 관측했다** — handoff 2세션째 「미검증」으로 이월되던 HTTP 라우팅 계층
  - <sub>`to-do.md:21` 의 라이브 검증은 2026-09-06 것으로, 제출서류 추출 재작성(`c1e4d6a`) 이전이다</sub>
  - `python3 src/server.py` 기동 → `GET /` **200 · 7,799B**
  - 시연 URL 3건 전부 `GET /api/analyze?url=…` **200 · ok=true · 0.3초** `[관측]`
  - 시연2(비타민스테이션) 필드 관측 — `minimumBidPrice="비공개"` · `countdown.label="입찰 시작 전 · 마감 D-23"` · `jointBidAllowed="불가능"` · `evictionResponsibility=""` · `docChecklist.status="attachment_only"` · `relatedDocs` 6건
  - 밤사이 넣은 공동입찰 축이 **라이브 경로에서도 값이 채워진다**. 저장 HTML 밖에서 확인한 첫 사례다
- **게이트 재실행** — 순수 함수 `153/153 · 축 21 · 미커버 0` `[관측]`
- **임시 공개 주소 개통** — `cloudflared` 퀵 터널 `https://sprint-hygiene-rivers-emails.trycloudflare.com`
  - 외부 경유 검증 `[관측]` — `/` 200(7,799B) · `/app.js` 200(61,137B) · `/styles.css` 200(20,821B) · `/api/analyze` **200 · 0.63초 · 로컬과 동일 값**
  - **ngrok 은 이 기기에서 못 쓴다** — `brew install --cask ngrok` 로 3.39.11 설치는 됐으나 authtoken 이 없어 `ERR_NGROK_4018` 로 세션이 열리지 않는다. 9/6 회의 때 쓴 것은 다른 기기다
    - ⚠️ **2026-09-08 해소** — authtoken 을 등록했다. 위 문장은 그날부터 사실이 아니다. `docs/60_본선준비/17_공개접속주소_ngrok_20260908.md`
- 시연 URL 3건은 **오늘도 살아 있다** — 마감이 2026-10-01 이후

### 미완료
- ~~**ngrok authtoken 미등록**~~ — **2026-09-08 등록 완료.** 고정 도메인 `https://overripe-astrology-impure.ngrok-free.dev` 로 개통했다. 상세 `docs/60_본선준비/17_공개접속주소_ngrok_20260908.md`
- 터널·서버 모두 **이 세션이 띄운 foreground 프로세스**다. 기기가 자거나 프로세스가 죽으면 주소가 사라지고, 재기동하면 주소가 바뀐다
  - ⚠️ **2026-09-08 정정** — 「재기동하면 주소가 바뀐다」는 틀렸다. 무료 요금제도 고정 도메인 1개가 붙어 재기동 후 같은 주소가 나온다(실측).
  - ⚠️ **2026-09-08 해소** — 서버·터널 모두 launchd 상시화했다. 세션과 무관하게 살아 있고 죽으면 자동 재시작된다.
- 첨부 PDF·HWP 파서 · 정답 서류 목록 · 공공데이터 API 경로는 직전 세션 상태 그대로

### 다음 시작점
- 상시 주소가 필요하면 ngrok authtoken 등록(무료 요금제도 고정 도메인 1개) 또는 `cloudflared` 명명 터널로 올린다
- 첨부 파서 도입 여부 결정 — B형 13건이 여기 걸려 있다

### 불안정 영역
- 퀵 터널 주소는 **인증이 없다.** 주소를 아는 사람은 누구나 연다. 링크를 아는 범위 밖으로 흘리지 않는다
- `notice` 의 키 이름이 문서 서술과 다르다 — `bidDeadline`·`minimumBidPrice` 이며 `deadline`·`minBidPrice` 가 아니다. 외부에서 필드를 물릴 때 주의

---
## [push 직전 재판정] 리뷰 반영분 `bf70656..7756bf7` — **GO** 2026-09-08 05:33 KST

### 완료
- **판정 대상** `7756bf7`(push 후보) · 커밋 3건 `faa24c2`(코드)·`165a03d`(문서)·`7756bf7`(handoff)
- **최종 판정 GO** — 반영 세션의 주장 8건을 **전부 독립 재현**. 과대주장·미달 0건
- **선행 리뷰 P1-A·P2-B·P2-C·P2-D 4건 모두 종료 조건 충족** `[관측]`
- **자작 변이 5건 전부 rc≠0** — 반영 세션의 M1·M2·M3 미사용. 신설 축 B11·B11-1 은 등가변이가 아니다
  - N1(「가능」 하드코딩)·N2(명도책임 값 탑재) — 게이트 rc=1 **+ 표본 rc=4**(배선 12/23·0/23)
  - N3(명도책임 「매수인」 하드코딩)·N4(action 분기 반전)·N5(「원문」 남긴 채 목록 전제 동사 복귀) — 게이트 rc=1
  - 변이마다 `~/Library/Caches/com.apple.python` 삭제 + `grep` 반영 확인 후 실행
- **P1-A 라이브 재측정** `[관측]` — 시연2·시연3 물건상세 **HTTP 200**(722,685B·720,884B) · 표본 #17·#22 공고상세 **500** · 시연2 에서 `pbctCdtnNo` 만 빼면 **500**
  - 선행 리뷰가 `[추론]` 으로 남긴 인과를 관측으로 바꿨고, 미검증이던 시연 URL 2건을 채웠다
- **P2-D 제품 판정** — `build_notice()` 실호출 + `samples.json` **관측값**(is_sale 강제 아님)으로 문구 관찰
  - #17·#18(서류 0건) 새 문구 · #1(서류 16건) 옛 문장 유지 · #21(임대) 항목 없음 — 전부 재현
  - reason 과 action 이 모순되지 않고, 「못 뽑았으면 못 뽑았다고 말한다」를 어기지 않는다
- **느슨해진 곳 없음** — 삭제된 단정 0건 · `check()` **132 → 144**(적기 직전 `grep -c`)
- 게이트 3종 자가 실행 — 순수 함수 **153/153 · 축 21 · 미커버 0** · 화면 **2 passed · 축 9 · 미커버 0** · 표본 rc=0 · 배선 **23/23**
- 산출물 `team-reports/2026-09-08-opada-checklist-review/push-rejudge-7756bf7.md` 1개. 레포 코드·문서 무수정

### 미완료
- **새 지적 P2-F** — `is_sale` 게이트가 **서류 근거가 있는** 임대 공고의 공동/대리 안내도 막는다
  - 표본 #4(국유재산/임대)는 서류 8건 · `conditionKeys` 에 `joint`·`proxy` 가 있는데 코치 항목이 안 뜬다
  - 이번 diff 가 만든 동작이 아니라 **기존 동작**이라 push 를 막지 않는다. `to-do.md` 등재 미착수
- **P2-E**(방어 코드 4곳 주석 표기)는 반영 범위에서 제외됐다 — 코드 행동 무변경이라 GO 를 막지 않는다

### 다음 시작점
- `7756bf7` push. 그 뒤 P2-F 를 `to-do.md` 에 등재하거나 `src/server.py:1160` 주석에 병기

### 불안정 영역
- **로컬 서버 `/api/analyze` 라이브 실호출은 2세션째 미검증** — 남은 공백은 HTTP 라우팅 계층 하나이고 이번 diff 는 그 계층을 건드리지 않았다(`src/server.py` 변경은 `local_ai_coach` 안 12줄)
- `build_notice` 의 `assetType`·`dispositionLabel` 은 저장 HTML 에서 안 뽑힌다 — 재현에 `samples.json` 관측값을 덮어썼다
- 명도책임 물건상세 경로는 구현 미착수 범위

---
## [리뷰 반영] P1 1건·P2 3건 닫기 — 코드 1커밋 · 문서 1커밋 2026-09-08 05:30 KST

### 완료
- **판정 대상** 리뷰 `bf70656` 의 §8 지적 4건(P1-A · P2-B · P2-C · P2-D). 새 기능 없음
- **① P1-A 닫음** — 「`itemDetail` 개통」 주장을 **물건상세 입력 경로**로 좁혔다
  - 실측 그대로 갈라 적음 — 물건상세 200(724,351B) / 공고상세 500(2/2) / 표본 `pbctCdtnNo` 0/23
  - `to-do.md` 항목을 `[x]` → `[ ]` 로 되돌리고 「공고상세 경로는 열린 결함」으로 표기
  - 종료 조건 실행 — `handoff.md`·`to-do.md` 의 `itemDetail` 줄 중 **조건 없는 줄 0건**
- **② P2-B 닫음** — `build_notice` 의 `jointBidAllowed` 배선에 단정을 넣었다
  - 축 **B11** 신설(`test_docs_extraction.py`) — `build_notice` 를 실제로 부른다. 네트워크 2개만 연기
  - 양성 1 + 음성 대조 3(값 뒤집기 · 절 없음 · 명도책임 무생성) + 도달 증거 1
  - `measure_sample_23.py` 에 `build_notice` 산출값 열 추가 — 불일치면 `rc=4`
- **③ P2-C 닫음** — 「증분 3건」을 **축 기준 3건 / 화면 코치 노출 2건**으로 갈라 5곳에 병기
- **④ P2-D 닫음** — 서류 0건 경로의 코치 `action` 을 「원문·첨부에서 직접 확인」으로 바꿨다
  - 축 **B11-1** 신설 — 양성 2 + 음성 대조 2(서류 근거 경로는 옛 문장 유지 · 두 경로가 실제로 다름)

### 실측 수치 `[관측]`
- 순수 함수 **153/153 · 축 21개 · 미커버 0** (141·19 → B11·B11-1 신설)
- 화면 **2 passed · 축 9개 · 미커버 0** (무변동 — 화면 축은 건드리지 않았다)
- 표본 23건 `rc=0` — 서류 **10/23**(A 8/8 · B 2/13 · C 0/2) · 첨부 **59/59**(공고 20건) · `joint` **12/23** · 못 읽음 0/23 · 명도책임 3/23
  - **움직인 값 0건.** 이번 수정은 문구와 검사만 건드렸고 판정식을 바꾸지 않았다
- **`build_notice` 배선 일치 23/23** · 판별 케이스 2건 — #17 공동`가능`/대리`불가능`→`가능` · #22 공동`불가능`/대리`가능`→`불가능`
- **킬러 변이 3건 전부 `rc≠0`** (`/tmp/opada-mut` 사본 · 캐시 삭제 후)
  - M1 `jointBidAllowed` 를 `""` 로 → 게이트 150/153
  - M2 `대리입찰` 값 탑재 → 게이트 150/153 · 표본 `rc=4` 배선 21/23 불일치 `[17,22]`
  - M3 `action` 분기 삭제 → 게이트 150/153

### 자작 종료 조건 (내가 만진 델타)
- **②·④용** `/tmp/own-exit.py` — 표본 23건을 `build_notice` → `local_ai_coach` 로 **끝까지** 태워 화면에 나가는 `action` 문장을 잰다. 리뷰 조건은 ② 를 `build_notice` 까지만, ④ 를 픽스처 1개로만 본다
  - 결과 **PASS** — 항목 발동 12/23 · 서류 근거 9건(3~16건 보유)은 옛 문장 · 서류 0건 3건(17·18·21)은 원문 확인 문구 · 양쪽 경로 모두 도달
  - 음성 대조 — M3 사본에 같은 조건을 돌려 `rc=1` 확인(무증명 아님)

### 미완료 / 미검증
- **push 안 함** — 커밋 2개 로컬(`faa24c2` 코드 · `165a03d` 문서)
- **P2-E 미착수** — 방어 코드 4곳(R4·R5·R6·R12) 주석 표기. 리뷰가 「결함 아님」으로 판정했고 이번 지시 범위 밖
- **공고상세 입력 시 `itemDetail` 링크 처리 방침 미결정** — 500 이 나가는 것을 알면서 링크를 유지 중이다. `to-do.md` 에 열린 항목으로 등재
- **자작 종료 조건의 `is_sale` 강제** — 23건 전부 `dispositionLabel="매각"` 으로 덮어 코치 분기를 열었다. 실제 화면 노출과 다르다(#21 은 임대). 코치 **분기**를 잰 것이지 화면 노출을 잰 것이 아니다
- **라이브 `/api/analyze` 실호출 없음** — 함수 수준 런타임 관찰로 대체

### 다음 시작점
- 리뷰어(regression-reviewer)에게 `bf70656..165a03d` 재판정 1회 — 저자=검증자 금지

### 불안정 영역
- **macOS python3.9 바이트코드 캐시** — 변이 실험마다 `~/Library/Caches/com.apple.python/` 를 지웠다. 안 지우면 원본 파일인데 변이 코드가 돈다(직전 리뷰가 허위 관측 1건 생성 후 철회)
- `notice_from_html()` 이 `server.fetch_onbid` 를 전역 치환한다. 예외 시 `finally` 로 되돌리지만 병렬 실행에는 안전하지 않다

---
## [독립 리뷰] 공동입찰 축 물림 재현 판정 — 조건부 GO 2026-09-08 05:15 KST

### 완료
- **판정 대상** `62279f8..0db865d`(커밋 3건). 코드 무수정 · 보고서 1개만 신설
  - `team-reports/2026-09-08-opada-checklist-review/joint-bid-independent-review.md`
- **최종 판정 조건부 GO** — 코드 2커밋은 GO, 문서 커밋 `0db865d` 의 P1 과대주장 1건을 좁히면 push 가능
- **구현 세션 주장 9건 중 8건 재현** `[관측]` — 게이트 3종을 리뷰어가 직접 재실행
  - 순수 함수 `141/141 · 축 19 · 미커버 0` · 화면 `2 passed · 축 9 · 미커버 0` · 표본 `rc=0`
  - `joint` 9/23 → 12/23 · 증분 **축 기준 3건**(17·18·21) / **화면 코치 노출 2건**(17·18 · #21 은 임대라 `is_sale` 게이트) · 반대 불일치 0/23 · 못 읽음 0/23 · 어휘 `{가능:12, 불가능:11}`
- **자작 변이 14건** — 구현 세션 스크립트 미사용. 붉힘 8 / 생존 6
  - 툴팁 오탐 경로(절 전체 훑기 · 문자열 존재만 검사)는 내가 새로 짠 변이로도 붉어진다 → 주장 5 성립
  - 문자열 존재만 검사하는 변이는 표본 화면 값을 23/23 으로 부풀린다(위험 크기 실측)
- **창 경계(`tit_area01`) 필요성 실측** — 값 없는 항목이 옆 값을 훔치는 경로가 표본에 실재(46쌍). 단 질의 라벨 2개는 23/23 자기 값 보유

### 새 지적 4건
- **P1-A** `itemDetail 개통` 주장이 코드보다 넓다 — 물건상세 입력 HTTP 200(724,351B) / 공고상세 입력 HTTP 500(2/2) `[관측]`. 표본의 `pbctCdtnNo` 보유 0/23
- **P2-B** `build_notice` 의 `jointBidAllowed` 배선에 단정 없음 — 변이 R13·R14 가 두 게이트 모두 통과. 배선 자체는 리뷰어 런타임 관찰 5/5 일치
- **P2-C** 「증분 3건」은 축 기준. 화면 코치 노출은 2건(#21 은 임대라 `is_sale` 게이트에 막힘)
- **P2-D** 서류 0건 경로의 코치 action 이 「필요한 서류만 남깁니다」로 남아 목록 존재를 전제

### 미완료
- `cltrHisNo` 5회 소거 실험 재현 · 시연 URL 나머지 2건 `itemDetail` — 시간 예산
- 로컬 서버 `/api/analyze` 실호출 — 라이브 fetch 필요, 함수 수준 런타임 관찰로 대체

### 다음 시작점
- P1-A 문구 축소 후 push. 종료 조건은 보고서 §8 에 지적별 1개씩 적어 뒀다

### 불안정 영역
- **macOS 시스템 python3.9 는 바이트코드를 `~/Library/Caches/com.apple.python/` 에 쓴다.** 변이가 원본과 바이트 수가 같으면 캐시가 낡아 **원본 파일인데 변이 코드가 실행된다.** 이번 리뷰에서 실제로 「배선이 대리입찰 값을 싣는다」는 허위 관측을 한 번 만들었고, 캐시 삭제 + `PYTHONDONTWRITEBYTECODE=1` 로 재현해 철회했다. 변이 실험 시 필수
- `build_notice` 의 `assetType` 이 저장 HTML 에서 `중메뉴 펼치기` 로 나온다(이번 diff 밖 · `measure_sample_23.py` 도 `samples.json` 관측값을 쓴다)

