# push 직전 최종 재판정 — `18f6d0b..6497261`

- 판정 대상 최종 커밋: **`6497261`** (push 에 들어갈 커밋)
- diff 범위: `git diff 18f6d0b..6497261` — 7파일 · +403 / −65 `[관측]`
- 선행 산출물: `review.md`(1차 · `4aedfb7`) · `rejudge.md`(재판정 · `18f6d0b`)
- 재판정자: 재판정 세션이 소실되어 대신 들어온 별개 검증자. 구현자 아님
- 기준·작성 시각: 2026-09-08 04:38~04:42 KST (`date` 출력)
- 실험 격리: 변이·계측은 전부 레포 밖 사본(`/tmp/fj` · `/tmp/mb_*` · `/tmp/ms_*`)에서만 수행
- 표본 원자료: `/tmp/onbid-survey/html/` **23건** 존재 `[관측]` · `samples.json` 로드됨

---

## ⑴ 한 줄 판정

**조건부 GO — push 해도 된다.**
코드 행동 변경 `5281b17` 은 주장대로 동작하고 제품으로서도 맞다.
신설 축 B7·S1 은 내가 설계한 변이로 무증명이 아님을 확인했다.
남은 조건 1건은 **`changelog.md` 의 단언 수 갱신 누락**이며 코드 되돌림을 요구하지 않는다.

---

## ⑵ 판정표

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| ① | `5281b17` 이 주장대로 동작하는가 | ✅ **재현** | 2/23 · 10/23 · 16/23 · 화면 위양성 0 · 정당 3 전부 일치 |
| ①' | 이 상태가 제품으로서 맞는가 | ✅ **맞다** | 표본 01 의 표 제출방법이 실제 `직접제출` · 안내 문구가 사실에 근거 |
| ② | B7·S1 이 무증명이 아닌가 | ✅ **증명됨** | 자작 변이 B7 2건 · S1 1건 rc≠0 · 전부 비등가 확인 |
| ③ | P1-A 전파 누락 | ✅ **없음** | 옛 값·옛 정의 양쪽 grep 0건 |
| ④ | P2-C 정정 수치 17/3/3 | ✅ **맞다** | 독립 계측으로 동일 분포 |
| ⑤ | 헤드라인 지표 | 🟡 **1건 어긋남** | 게이트는 **119/119 · 축 16개**(지시의 119/119 와 일치) · 그러나 `changelog.md` 는 114·15 로 낡음 |
| ⑥ | 3차 반영이 느슨하게 만들었나 | ✅ **아니오** | 단언 삭제 0건 · `check()` 106→111 · `await expect` 42→48 |

---

## ① `5281b17` — 동작 재현과 제품 판단

### ①-a 세 안의 값 — 재현됨 `[관측]`

`/tmp/fj_indep.py` 를 내가 새로 작성해 표본 23건에 돌렸다.
`build_notice()` 의 `disposition` 유도식(hidden input → `op_cm_badge` → API)을 그대로 옮겼고, API 경로는 인증키 부재로 생략했다.

```
항목만       has_direct_submit= 2/23 [14, 20]                        화면노출=[]        위양성=[] 정당=[]
중간안(현행) has_direct_submit=10/23 [1..8, 14, 20]                  화면노출=[1,2,3]   위양성=[] 정당=[1,2,3]
무가드(옛)   has_direct_submit=16/23 [1..10, 14, 18, 20, 21, 22, 23] 화면노출=[1,2,3]   위양성=[] 정당=[1,2,3]
```

- 커밋 메시지의 표(2/23 · 10/23 · 16/23 · 위양성 0/0/0 · 정당 0/3/3)와 **전건 일치**한다.
- `is_sale=True` 는 `[1, 2, 3]` 3건뿐이다 `[관측]`. 재판정의 관측과 같다.
- 위양성 판정 기준은 「화면에 뜨는데 서류 0건」이다. 중간안의 화면 노출 3건은 전부 서류 16건짜리다.

### ①-b 의도대로 동작하는가 — **예** `[관측]`

- `bool(checklist_items)` 가드가 서류 0건 경로를 막는다. 표본에서 서류 0건이면서 화면에 뜬 건은 0건.
- 게이트 B7 음성 대조(「서류 0건이면 표가 있어도 안 뜬다」)가 그 경계를 코드로 고정한다.

### ①-c 이 상태가 제품으로서 맞는가 — **맞다** `[관측]`

표본 01(압류재산)을 프로덕션 `local_ai_coach` 에 그대로 통과시켜 화면 문구를 직접 관찰했다.

```
headline: AI 누락 점검: 공고가 말하지 않는 빈칸
 - 공동/대리입찰 서류가 나에게 필요한지        source=제출서류 표
 - 직접제출 서류의 실제 마감 시각               source=제출방법   ← 되살아난 항목
 - 보증금·잔금·추가 비용의 납부 순서            source=가격/입찰정보
askAgency: … 직접제출 서류의 접수처, 접수 가능 시간, 우편/온라인 대체 가능 여부 …
```

- 그 근거가 실재하는지 대조했다 — 표본 01 의 `tableRows` 제출방법 값 집합은 **`['직접제출']`** 이다 `[관측]`.
- 즉 화면 문구는 원문에 있는 사실을 가리킨다. 거짓 안내가 아니다.
- 문구는 「확인하십시오」 계열이고 입찰 여부·법률·수익성 판단을 담지 않는다 — 금지표현 가드레일 위반 없음 `[관측]`.

### ①-d 중간안 채택 판단의 타당성 — **타당하다**

- 재판정이 남긴 것은 「사용자 판단」이었으나, 그 재판정 자신이 「표본 기준으로는 중간안이 현행보다 낫다」고 적었다 `[인용]`.
- 실측은 그 서술과 일치한다 — 얻는 것 3건, 잃는 것 0건.
- 되돌리기 비용도 낮다. 커밋이 분리돼 있어 `revert 5281b17` 하나로 원복된다 `[관측]`.
- 다만 이 판단은 **저장 HTML 23건 기준**이다. 라이브에서 `is_sale=True` 가 늘면 위양성도 늘 수 있다 `[추론]` — 재판정이 남긴 미검증 항목 그대로다.

---

## ② 신설 축 B7·S1 — 내가 설계한 변이로 검증

3차 세션의 변이(`/tmp/mut3a`·`/tmp/mut3b`·`/tmp/mut4`·`/tmp/mut4b`)는 **쓰지 않았다.** 앵커를 새로 잡았다.

### ②-a B7 (코치 배선) — 자작 변이 3종 `[관측]`

| 내 변이 | 형태 | 게이트 | 비등가 확인 |
|---|---|---|---|
| `b_invert` | `bool(checklist_items)` → `not checklist_items` | **rc=1** · 116/119 | ✅ 양성·음성 **둘 다** FAIL · 화면 노출 `[1,2,3]`→`[]` |
| `b_wrongkey` | `checklist.get("tableRows")` → `get("tblRows")` | **rc=1** · 118/119 | ✅ 양성 FAIL · 화면 노출 `[1,2,3]`→`[]` |
| `b_all` | `any(row…)` → `all(row…)` | rc=0 · 119/119 | 🟢 **등가변이** — 표본 결과·화면 노출 전부 불변 |

- `b_invert` 는 가드를 뒤집어 **양성과 음성을 동시에** 붉힌다. B7 이 한쪽만 재는 검사가 아님을 보인다.
- `b_all` 이 초록인 것은 **결함이 아니다.** 표본 23건에서 결과가 전혀 바뀌지 않는 등가변이임을 직접 확인했다(중간안 10/23 · 화면 `[1,2,3]` 동일) — 검사가 못 잡은 것이 아니라 잡을 것이 없다.
- 도달 증거 3건(서류 1건↑ · 항목 method 전건 없음 · 표에 제출방법 있음)이 함께 단정돼 있어, 픽스처가 분기를 벗어나면 영구 초록이 되지 않는다 `[관측]`.

**B7 은 무증명이 아니다.**

### ②-b S1 (배지 색) — 자작 변이 `[관측]`

3차 세션은 `safe` 고정 / `warn` 고정을 썼다. 나는 **색을 뒤바꾸는** 변이를 썼다 — 실제로 일어나기 쉬운 회귀 형태다.

```
s_swap: src/app.js  profile.code === "A" ? "safe" : "warn"
                 →  profile.code === "A" ? "warn" : "safe"   (앵커 2곳 전부)

$ npx playwright test tests/screen.spec.js   →  2 failed
  붉어진 자리: B형 toHaveClass(/\bwarn\b/)
  Received string: "badge safe"   ← 렌더된 클래스가 실제로 바뀌었다
```

- **2 failed** 이므로 A형·B형 양쪽 단언이 모두 감지기로 동작한다.
- Playwright 가 실제 DOM 에서 `<span class="badge safe">첨부를 봐야 함</span>` 을 읽었다 — **등가변이가 아니다** `[관측]`.
- 기준선은 **2 passed** 로 재현했다 `[관측]`.

**S1 은 무증명이 아니다.**

---

## ③ P1-A 전파 — 누락 없음 `[관측]`

옛 **값**과 옛 **정의** 양쪽으로 grep 했다.

```
$ grep -rn "99단언|킬러 변이 39|표본 09·10·18·21·22·23|위양성 6" src/ tests/ changelog.md to-do.md handoff.md docs/
to-do.md:89    「위양성 6건」… 전부 is_sale=False 라 화면에 뜬 적이 없다   ← 같은 줄에 정정 단서
handoff.md:18  종료 조건 실행 기록                                         ← 정정 사실을 적은 줄
handoff.md:57  「옛 코드에서도 화면에 뜬 적이 없다」                        ← 정정 내용
handoff.md:59  「귀속이 틀렸다 — 정정 대상」                                ← 정정 내용
handoff.md:61  「99단언 … 으로 낡았다」                                     ← 정정 대상을 지목한 줄
```

- `src/` · `to-do.md` · `changelog.md` 에 옛 주장이 **살아 있는 자리 0건**.
- `handoff.md` 5건은 전부 「그것이 틀렸다」를 적은 정정 서술이며, 낡은 주장으로 읽히지 않는다.
- `src/server.py:1086-1094` 의 새 주석은 「항목만 2/23 · 중간안 10/23 · 무가드 16/23」과 「위양성 0/0/0 · 정당 0/3/3」을 적었고, 내 실측과 **일치**한다 `[관측]`.

---

## ④ P2-C 정정 수치 — **맞다** `[관측]`

독립 계측 결과:

```
처분방식 분포: {'매각/공매': 3, '임대': 3, '처분방식 확인': 17}   합계 23
is_sale=True: [1, 2, 3]  (3건)
```

- 3차 세션의 정정(`처분방식 확인` 17 · 임대 3 · 매각/공매 3)이 **정확하다.**
- 재판정의 「20/23 이 `처분방식 확인`」은 오기였고, 20 은 `is_sale=False` 공고 수(23−3)라는 설명도 맞다 — `is_sale=True` 3건의 여집합이다 `[관측]`.
- 이 로직은 이번 diff 가 건드리지 않았다 `[관측]`. 기록만 신설한 것이 맞다.

---

## ⑤ 헤드라인 지표 재현

전부 `/tmp/fj` 사본에서 직접 실행했다 `[관측]`.

| 지표 | 지시의 값 | 실측 | 판정 |
|---|---|---|---|
| 구체 서류명 추출 | 10/23 (A 8/8 · B 2/13 · C 0/2) | **10/23 · A 8/8 · B 2/13 · C 0/2** | ✅ |
| 첨부 파일 단위 | 59파일 | **59/59** | ✅ |
| 첨부 공고 단위 | 20공고 | **첨부 있는 공고 20건** | ✅ |
| 순수 함수 게이트 | 119/119 | **119/119 · 연기 3개 · 축 16개 중 미커버 0개** | ✅ |
| 화면 게이트 | 2/2 | **2 passed** | ✅ |
| 조건 축 | 10 | `len(server.CONDITION_AXES)` = **10** | ✅ |

- 계측 첫 줄이 「재산유형 출처: samples.json 관측값 · 사본 표와 일치」다 — 사본 표 fallback 으로 떨어지지 않았다 `[관측]`.
- **어긋난 자리 1건** — `changelog.md:53` 은 「2026-09-08 실측은 순수 함수 **114단언·15축**」이라고 단정한다. 실측은 **119단언·16축**이다 → 아래 P2-D.

---

## ⑥ 3차 반영이 느슨하게 만들었는가 — 아니오

`git diff 18f6d0b..6497261 -- tests/ src/` 의 삭제 라인을 **전수**로 봤다 `[관측]`. 총 5줄이다.

- 3줄 — `has_direct_submit` 옛 주석(귀속 오류 포함). 더 넓은 주석으로 대체.
- 1줄 — `has_direct_submit` 판정식. 더 넓은 식으로 대체.
- 1줄 — `screen.spec.js` 의 S1 축 표 행. 더 넓은 케이스·음성 대조 문구로 대체.

**단언 삭제·약화 0건** `[관측]`:

```
check(...)      18f6d0b 106개 → 6497261 111개  (+5)
await expect    18f6d0b  42개 → 6497261  48개  (+6)
```

「이제 실패할 수 있는가」와 「옆에 새 검사가 생겼는가」 구분:

- **이제 실패할 수 있게 된 것**
  - 「서류 0건이면 표가 있어도 직접제출 안내가 뜨지 않는다」 — B7 음성 대조. 2차 이전에는 **어떤 검사도 이 경계를 재지 않았다.** 내 `b_invert` 변이에서 실제로 붉어진다 `[관측]`.
  - 「A형 배지는 `safe`, B형 배지는 `warn`」 — S1 색 단언. 내 `s_swap` 변이에서 2 failed `[관측]`.
- **옆에 새 검사만 생긴 것** — 없다.
- **판정식이 넓어진 것은 사실이다** — `has_direct_submit` 이 참이 되는 표본이 2→10 으로 늘었다. 다만 넓어진 경계 자체를 B7 음성 대조가 새로 고정하므로, 「검사 없이 느슨해진」 자리는 아니다.

---

## ⑺ 새 지적

### P2-D. `changelog.md:53` 의 단언 수·축 수가 다시 낡았다

- 위치: `changelog.md:53`
- 현 문장: 「단언 수는 게이트 실행 출력이 SoT 이고, 2026-09-08 실측은 순수 함수 **114단언·15축**이다.」
- 실측: **119단언 · 16축** `[관측]` (`python3 tests/test_docs_extraction.py` → `119/119 통과 · 연기 3개 · 축 16개 중 미커버 0개`)
- 경위: `3b94de1` 이 99→114 로 고쳤고, 그 뒤 `5281b17` 이 B7(축 +1 · 단언 +5)을 넣으면서 같은 커밋에서 전파하지 않았다 `[관측]`.
- 등급 근거 **P2** — 코드 행동에 영향 없고, `handoff.md` 미완료 절에 「`changelog.md` 미갱신」이 이미 적혀 있다 `[관측]`. 다만 changelog 본문은 그 수치를 「실측」으로 단정하므로 다음 사람이 그대로 인용한다.
- **최종 커밋에서 확인할 종료 조건 1개**
  > `grep -n "114단언\|15축" changelog.md` 가 **0건**이고, 그 자리의 수치가 `python3 tests/test_docs_extraction.py` 출력의 분모(**119**)·축 수(**16**)와 일치할 것.

> 그 밖의 새 지적 없음. P0 · P1 **0건**.

---

## ⑻ 재현하지 못한 것과 이유

| 항목 | 이유 |
|---|---|
| 라이브 온비드에서의 `is_sale` 분포 | 저장 HTML 23건으로만 측정. 라이브에서 `dspsMthodNm`·배지가 채워지면 `is_sale=True` 가 늘고 위양성도 늘 수 있다 `[추론]` |
| 공공데이터 API 6종 | `ONBID_API_SERVICE_KEY` 부재. `disposition` 3순위(API) 경로를 내 계측에서 생략했다 — 표본 분포는 그 경로 없이도 프로덕션과 같다(키가 없으면 프로덕션도 같은 경로) `[관측]` |
| 실 URL e2e | 네트워크 의존이라 이번 회차 미실행. `screen.spec.js` 2/2 만 돌렸다 |
| 3차 세션의 자작 종료 조건 `/tmp/exit_cond_3.py` | 레포 밖 파일이라 원본 미확인. 같은 취지의 검증을 **내 스크립트로 대체 수행**했다(①-a) |
| 첨부 파일 내부(PDF·HWP) | 범위 밖 · to-do 미결 |
| `foreign` 조건 축 존치 · CI 도입 | 동원님 판단 사항 · 범위 밖 |

---

## ⑼ 검증 경계

| 구분 | 상태 |
|---|---|
| 순수 함수 게이트 | ✅ 119/119 재현 · 자작 B7 변이 2/3 rc≠0 · 나머지 1건은 등가변이로 확인 |
| 화면 노출 게이트 | ✅ 2/2 재현 · 자작 S1 변이 rc≠0(2 failed) · 비등가 확인 |
| 표본 23건 계측 | ✅ 10/23 · 59/59 · 20공고 · 처분방식 3/3/17 전부 독립 재현 |
| `has_direct_submit` 세 안 | ✅ 2/23 · 10/23 · 16/23 · 화면 출력 직접 관찰 |
| 제품 판단(화면 문구) | ✅ 표본 01 실제 렌더 관찰 · 표 제출방법 `['직접제출']` 대조 |
| 금지표현 가드레일 | ✅ 되살아난 문구에 입찰·법률·수익성 판단 없음 |
| 라이브 온비드 경로 | 🔴 미검증 — 저장 HTML 기준 |
| 공공데이터 API 6종 | 🔴 인증키 부재로 미검증 |
| 실 URL e2e | 🔴 이번 회차 미실행 |
| 수치 전파 | 🔴 `changelog.md:53` 「114단언·15축」 미갱신(P2-D) |
| 열거 대조 | 조건 축 코드 **10** = `server.py:252` **10** = `changelog.md:46` **10** — 일치 |
| 워킹트리 | ✅ 재판정 전 구간 `git status --porcelain` **빈 출력**. 변이·계측은 전부 `/tmp` 사본 |

---

## ⑽ 최종 판정

**조건부 GO — push 해도 된다.**

- **P0 0건 · P1 0건 · P2 1건**(P2-D).
- 코드 행동 변경 `5281b17` 은 재현되고, 화면 문구가 원문 사실에 근거하며, 되돌림 비용이 낮다. **중간안 채택 판단은 타당하다.**
- 신설 축 B7·S1 은 내가 새로 설계한 변이로 rc≠0 을 실측했고 비등가임을 확인했다 — **무증명이 아니다.**
- 남은 조건 1건은 `changelog.md` 한 줄의 수치 갱신이며, 코드 되돌림도 push 보류도 요구하지 않는다.
