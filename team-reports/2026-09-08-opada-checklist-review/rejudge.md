# 2차 반영분 재판정 — `4aedfb7..18f6d0b`

- 판정 대상 최종 커밋: **`18f6d0b`** (push 예정 커밋)
- 재판정 diff 범위: `git diff 4aedfb7..18f6d0b` — 9파일 · +266 / −83
- 1차 리뷰: `team-reports/2026-09-08-opada-checklist-review/review.md`(`4aedfb7`) · 조건부 GO · P0 0 · P1 7 · P2 8
- 재판정자: 1차 리뷰어 세션이 소실되어 대신 들어온 별개 검증자. 구현자 아님
- 기준 시각: 2026-09-08 00:50~00:56 KST · 작성 시각: 2026-09-08 00:56 KST
- 실험 격리: 변이·계측은 전부 레포 밖 사본(`/tmp/rj` · `/tmp/rj-old` · `/tmp/mut_*`)에서만 수행
- 표본 원자료: `/tmp/onbid-survey/html/` 23건 존재 `[관측]`. `~/onbid-survey-backup/html/` 은 0건이라 쓰지 않았다

> **파일 위치 안내** — 지시는 `docs/reviews/` 였으나 그 디렉터리는 이 레포에 없다 `[관측]`.
> 1차 리뷰 실제 위치가 `team-reports/2026-09-08-opada-checklist-review/review.md` 이므로 같은 폴더에 새 파일로 두었다. 1차 파일은 손대지 않았다.

---

## ⑴ 한 줄 판정

**조건부 GO** — 1차 리뷰가 건 종료 조건 4개는 내 손으로 설계한 변이로 4/4 닫혔고 헤드라인 지표 6개도 전부 재현됐다. 다만 P1-7 반영의 **근거 서술이 표본 관측과 어긋나고**, 표본 기준으로는 화면에 뜨던 정당한 안내 3건을 지우고 화면에 뜨던 위양성은 0건을 닫았다. 코드 되돌림 없이 **근거 문장 정정 + 동원님 판단 1건**을 붙이면 push 가능하다.

---

## ⑵ 판정표

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| ① | `has_direct_submit` 델타의 제품 판단 | 🔴 **P1 신규** | 6/8 분해는 재현. 그러나 6건은 화면에 뜬 적이 없고, 지워진 8건 중 3건은 화면에 뜨던 정당한 안내 |
| ② | 1차 리뷰 종료 조건 4개 | ✅ **닫힘** | 자작 변이 4/4 rc≠0. 3건은 비등가 확인, 1건(P1-4)은 표본 등가 |
| ③ | 2차 반영이 느슨하게 만들었나 | ✅ **아니오** | 단언 삭제 0건. 삭제 라인은 docstring → 런타임 출력 이전. 다만 화면 축 표에 과대 선언 1건(P2) |
| ④ | 헤드라인 지표 재측정 | ✅ **전부 재현** | 10/23 · A8/8 · B2/13 · C0/2 · 59/59 · 20/20 · 114/114 · 축 10 |
| ⑤ | 미커버 축 | 🟡 **선언 0개 · 실측 2개** | 게이트는 「미커버 0」을 찍지만, 화면 S1 배지 색과 실 공고 형태의 코치 배선이 비어 있다 |

---

## ① `has_direct_submit` 델타 — 재현과 제품 판단

### ①-a 2차 세션의 6/8 분해 — **재현됨** `[관측]`

레포 밖 사본에서 세 안을 같은 표본 23건에 돌렸다(`/tmp/rj/tests/rj_direct_submit.py`, 레포에 넣지 않음).

```
안       참      위양성(서류 0건)                  정당(서류 1건 이상)
현행      2/23   0건 []                            2건 [14, 20]
중간안   10/23   0건 []                           10건 [1..8, 14, 20]
옛       16/23   6건 [9, 10, 18, 21, 22, 23]      10건 [1..8, 14, 20]
```

- 옛 16/23 → 현행 2/23, 닫힌 14건 = 위양성 6 + 정당 8. **2차 세션 주장 그대로다.**
- 위양성 6건의 번호(09·10·18·21·22·23)도 일치한다.

### ①-b 그런데 그 6건은 **화면에 뜬 적이 없다** `[관측]`

`has_direct_submit` 은 `local_ai_coach` 의 `if is_sale:` 분기 안에서만 소비된다(`src/server.py:1116`). 표본 23건의 `is_sale` 분포는 **True 3건(01·02·03 압류재산) / False 20건**이다.

옛 코드 사본(`/tmp/rj-old`)과 현행(`/tmp/rj`)에 같은 표본을 넣고 `unresolvedChecks` 를 직접 찍었다.

```
--- 표본01 압류재산 처분=매각/공매 is_sale=True 서류=16   (옛)
   [0] 공동/대리입찰 서류가 나에게 필요한지
   [1] 직접제출 서류의 실제 마감 시각      ← 여기
   [2] 보증금·잔금·추가 비용의 납부 순서
--- 표본01 (현행)
   [0] 공동/대리입찰 서류가 나에게 필요한지
   [1] 보증금·잔금·추가 비용의 납부 순서   ← 사라졌다

--- 표본09 공유재산 처분=처분방식 확인 is_sale=False 서류=0   (옛)
   [0] 서류 목록을 자동으로 뽑지 못했습니다
   [1] 내 업종으로 실제 사용 가능한지
   [2] 현장 상태와 인수 범위                ← 「직접제출」 항목 없음
--- 표본09 (현행) : 동일
```

- 표본09 는 **옛 코드에서도** 그 항목이 화면에 없다. `else:`(임대) 분기로 가고 `unresolvedChecks[:3]` 에서 잘린다.
- 즉 「서류 0건인데 직접제출을 전제하던 6건」은 **계산은 되었으나 렌더되지 않는 값**이었다.

### ①-c 세 안의 화면 노출 기준 분해 `[관측]`

| 안 | 값 | 화면에 뜬 위양성 | 화면에 뜬 정당한 안내 |
|---|---|---|---|
| 현행 `18f6d0b` | 2/23 | **0건** | **0건** |
| 중간안(to-do) | 10/23 | 0건 | **3건** (01·02·03) |
| 옛 `4aedfb7` | 16/23 | **0건** | 3건 (01·02·03) |

- 표본 기준으로 이 수정은 **위양성 0건을 닫고 정당한 안내 3건을 지웠다** — 순손실이다.
- 01·02·03 은 서류 16건을 실제로 뽑았고 온비드 표 제출방법 칸이 「직접제출」인 압류재산 공고다. 시연 기본 경로다.
- 부수 관측: 표본 01~08 은 `items` 의 `method` 가 전건 비어 있다(`item=[]`). A형(압류·국유)에서는 항목 단위 제출방법이 채워지지 않으므로, 현행 근거로는 **A형에서 이 항목이 구조적으로 뜨지 않는다.**

### ①-d 두 질문에 따로 답한다

- **의도대로 동작하는가** — 예 `[관측]`. 「서류 0건인데 직접제출을 전제」하는 계산 경로는 닫혔고, 게이트는 옛 코드로 되돌리면 붉어진다(`/tmp/rj-old` → 113/114).
- **이 상태가 제품으로서 맞는가** — **아니오** `[관측]`. 표본에서 얻은 것은 0, 잃은 것은 3이다. `to-do.md` 의 중간안은 그 3건을 되살리면서 위양성을 0으로 유지한다(측정값 10/23).

### ①-e 근거 서술의 사실 오류

`src/server.py:1089-1091` 주석과 `to-do.md` 의 근거 문장이 이렇게 적혀 있다.

> 서류를 한 건도 못 뽑아 「못 뽑았습니다」를 띄운 공고에서 바로 다음 줄이 「직접제출 서류」의 존재를 전제한다(표본 09·10·18·21·22·23 실측).

표본 09 를 옛 코드로 렌더한 결과에 그 줄은 **없다** `[관측]`. 그 6건은 `is_sale=False` 라 해당 분기에 도달하지 않는다. 1차 리뷰 P1-7 이 관측한 출력은 `is_sale=True` + 서류 0건인 별도 입력이며, 그런 조합은 표본 23건에 없다.

- 위험 자체는 **실재한다** `[추론]` — 매각 공고인데 서류를 못 뽑는 조합은 프로덕션에서 가능하다.
- 틀린 것은 「표본 6건 실측」이라는 **귀속**이다. 프로덕션 코드 주석에 박혀 있어 다음 사람이 그대로 인용한다.

---

## ② 1차 리뷰 종료 조건 4개 — 내 손으로 닫음

1차 리뷰의 종료 조건: 「`/tmp` 사본에서 변이 4개를 넣었을 때 `python3 tests/test_docs_extraction.py` 가 4/4 rc≠0」.
2차 세션 변이 스크립트는 쓰지 않고 내가 직접 앵커를 잡아 `/tmp/mut_*` 사본에 넣었다(`/tmp/mut_rj.py`).

| 변이 | 게이트 | 비등가 확인 |
|---|---|---|
| P1-1 `authorizing = list(conditions)` | **rc=1** · 113/114 | ✅ 표본 **10/23 → 11/23**(B형 2→3) `[관측]` |
| P1-2 `if key in seen:` (빈 파라미터 가드 해제) | **rc=1** · 112/114 | ✅ `fn_chkPdfRead('','3','','x.pdf','')` → 현행 `[]` / 변이 `dnldFile.do?atchSn=3` 깨진 URL 생성 `[관측]` |
| P1-3 C형 headline → 「서류 준비를 진행하십시오.」 | **rc=1** · 112/114 | ✅ 표본 18·19 의 headline 문자열이 실제로 바뀐다 `[관측]` |
| P1-4 `carried_applies = bool(carried)` | **rc=1** · 113/114 | 🟡 **표본 23건 결과 불변** — 경계 입력에서만 관측된다 |

- **4/4 rc≠0 — 종료 조건 닫힘** `[관측]`.
- P1-3 은 앵커가 466·472행 두 곳이라(`C형` 분기와 `not_found` 분기가 같은 문자열) **466행 한정**으로 넣어야 조건을 만족한다. 두 곳을 함께 바꾸면 앵커 모호로 스크립트가 멈춘다 — 다음 사람을 위해 남긴다.
- P1-4 는 1차 리뷰가 이미 「등가에 가깝다」고 적었고 내 측정도 같다. 게이트는 붉어지지만 **표본이 이 경계에 닿지 않는다**는 사실은 그대로다.

부속 조건(P2-5) — 축 개수 일치 `[관측]`:

```
len(server.CONDITION_AXES) = 10
src/server.py:252  「조건 축은 아래 10개다」
changelog.md:46    「조건 축 10개로 서류를 분기하며」
```

3곳 일치. **닫힘.**

---

## ③ 2차 반영이 느슨하게 만들었는가 — 아니오(단서 1건)

`git diff 4aedfb7..18f6d0b -- tests/` 의 삭제 라인을 전수로 봤다 `[관측]`.

- `tests/test_docs_extraction.py` −27줄: **전부 docstring**(PLAYED·AXES 표·요약 print 2줄)이다. **단언 삭제 0건.** 같은 내용이 실행 출력으로 옮겨졌다.
- `tests/screen.spec.js` −15줄: 마찬가지로 docstring → `test.beforeAll` 의 상수·출력. **단언 삭제 0건.**
- 「이제 실패할 수 있는가」와 「옆에 새 검사가 생겼는가」 구분:
  - **이제 실패할 수 있게 된 것**: P1-1·P1-2·P1-3·P1-4 의 음성 대조 4건. 위 표대로 각각 그 변이에서 실제로 붉어진다 `[관측]`.
  - **옆에 새 검사만 생긴 것**: 없음. 기존 검사를 약화시켜 새 검사로 덮은 자리는 발견되지 않았다.
- P1-5(용어 안내 축소)는 **삭제이지 약화가 아니다.** 회의록 원문을 대조했다 `[관측]` — `docs/60_본선준비/11_캠코_회의록_20260907.md:265` 에 정인기 차장의 「임대도 마찬가지입니다 … 최저 입찰 가격이나 보증금은 동일하게 보시면 됩니다」가 실재한다. 1차 리뷰가 `[미확인]` 으로 남긴 자리를 2차가 근거 있음으로 확인하고도 오해 소지를 이유로 뺐으며, 주석에 그 경위를 적었다. 판단으로 타당하다.

**단서** — 화면 축 표 `S1` 이 「커버 Y」로 선언돼 있으나, 1차 리뷰 P2-3(A/B 배지 색을 `safe` 고정으로 바꿔도 초록)은 미해결이다. 축 표가 실제보다 넓게 주장한다 → P2-B.

---

## ④ 헤드라인 지표 재측정 — 전부 재현 `[관측]`

전부 `/tmp/rj` 사본에서 직접 실행했다.

```
$ ONBID_SAMPLE_HTML_DIR=/tmp/onbid-survey/html python3 tests/measure_sample_23.py
구체 서류명 1건 이상 추출: 10/23
  A형: 8/8    B형: 2/13    C형: 0/2
첨부 다운로드 파라미터: 59/59 (첨부가 있는 공고 20건)

$ python3 tests/test_docs_extraction.py
114/114 통과 · 연기 3개 · 축 15개 중 미커버 0개

$ npx playwright test tests/screen.spec.js
2 passed

$ python3 -c "len(server.CONDITION_AXES)"
10
```

- 공고 단위 20/20 도 직접 셌다 — 「첨부 있는 공고 20건 · 전 파일 URL 확보 공고 20건」 `[관측]`. `changelog.md`·`to-do.md` 가 분모를 갈라 적은 것(P2-2 반영)은 실측과 맞는다.
- `package.json` 에 `test:unit`(P2-8 반영)이 생겼다. `npm test` = unit + e2e `[관측]`.
- 1차 리뷰가 미검증으로 남긴 `samples.json` 관측값 경로(`b2e2b46`)도 이번에 **발동했다** `[관측]` — 계측 첫 줄이 「재산유형 출처: samples.json 관측값 · 사본 표와 일치」다. 사본 표로 떨어지지 않았으므로 위 10/23 은 관측값 기준이다.

---

## ⑤ 미커버 축 — 선언 0개 · 실측 2개

두 게이트 모두 `PLAYED` / `AXES` / 미커버 축 이름을 **실행 출력으로 찍는다** `[관측]`. 순수 함수 게이트는 연기 3개·축 15개, 화면 게이트는 연기 2개·축 8개를 찍고 둘 다 「미커버 축: 없음」으로 끝난다. P2-6 은 닫혔다.

그러나 「미커버 0」은 **자기 선언**이다. 실측으로 비어 있는 자리 2건:

| 미커버 축 | 제품 위험 | 근거 |
|---|---|---|
| 화면 S1 의 배지 **색** | 낮음 | 텍스트는 단언되고 색만 무증명. 시각 신호에 그친다(1차 P2-3 그대로) |
| 코치 배선의 **실 공고 형태** | **높음** | 게이트는 합성 픽스처로 양방향(옛 복원 · 항상 False) 모두 붉히지만 `[관측]`, 표본 01~08 형태(서류 뽑힘 + 항목 method 없음 + 표 method 「직접제출」)를 재는 검사가 없다. 그래서 ①의 회귀가 일어나고도 114/114 초록이다 |

---

## ⑹ 신규 지적

### P1-A. `has_direct_submit` 축소가 표본 기준 순손실이고, 근거 서술이 사실과 다르다

- 위치: `src/server.py:1089-1092`(주석) · `to-do.md` 「`has_direct_submit` 근거 축소의 후속 판단」
- 내용: 위 ①-b·①-c·①-e. 닫힌 위양성 6건은 화면에 뜬 적이 없고, 지워진 8건 중 3건(01·02·03)은 화면에 뜨던 정당한 안내다.
- 등급 근거: 사용자에게 거짓을 말하지 않고 항목이 사라질 뿐이라 P0 이 아니다. 다만 시연 기본 경로(압류재산)에서 안내가 사라졌고 프로덕션 주석의 귀속이 틀렸다.
- **최종 커밋에서 확인할 종료 조건 1개**
  > `src/server.py` 의 `has_direct_submit` 주석에서 「표본 09·10·18·21·22·23 실측」 귀속을 지우거나 「그 6건은 `is_sale=False` 라 화면에는 뜨지 않았고, 닫은 것은 계산 경로다」로 정정한다. 정정 뒤 `grep -n "표본 09·10·18·21·22·23" src/ to-do.md` 가 **0건**이거나, 남은 자리마다 「계산 경로 · 화면 미노출」 단서가 같은 줄에 붙어 있어야 한다.

### P1-B. 중간안 채택 여부가 미결인 채 push 된다 — 동원님 판단 사항

- `to-do.md` 에 열려 있으나 **선택지별 실측 수치가 없다.** 이 재판정이 낸 표(①-c)를 그 항목에 옮기면 판단 재료가 갖춰진다.
- 중간안은 `docChecklist["items"]` 가 비어 있지 않을 때에 한해 `tableRows` 의 제출방법 칸도 인정하는 형태다. 측정값 **10/23 · 위양성 0 · 화면 노출 정당 3건 복원**.
- **종료 조건 1개**
  > `to-do.md` 의 해당 항목에 「현행 2/23 · 중간안 10/23 · 옛 16/23」과 「화면 노출 기준 위양성 0/0/0 · 정당 0/3/3」 두 줄이 들어가 있고, 채택 여부에 동원님 결정이 표시돼 있을 것.

### P2-A. `changelog.md:53` 이 낡았다

- 현 문장: 「순수 함수(`tests/test_docs_extraction.py` **99단언**·15축) … 킬러 변이 **39개**(서버 22 · 화면 17) 전부가 붉어지는 것을 실측했다」
- 실측은 **114단언** `[관측]`. 「킬러 변이 39개 전부 붉어짐」은 1차 리뷰가 **재현 불가**로 판정한 바로 그 주장이고, 2차 세션은 같은 문장을 `test_docs_extraction.py` docstring 에서는 지웠으나 changelog 에는 남겼다 `[관측]`.
- `handoff.md:55` 의 같은 문장은 **과거 세션 블록**이라 대상 밖으로 본다.
- **종료 조건 1개**
  > `grep -n "99단언\|킬러 변이 39" changelog.md` 가 0건이고, 대체 문장의 단언 수가 `python3 tests/test_docs_extraction.py` 출력의 분모와 일치할 것.

### P2-B. 화면 축 표 `S1` 의 「커버 Y」가 실제보다 넓다

- `tests/screen.spec.js:23` 의 S1 은 배지 **텍스트**만 단언하고 **색**은 무증명이다(1차 P2-3 미해결).
- **종료 조건 1개**
  > S1 의 커버 칸이 `Y` 가 아니라 색 미커버를 드러내는 값이거나, 배지 클래스(`safe`/`warn`)를 단언하는 케이스가 추가돼 「A형 배지 색 `safe` 고정」 변이에서 화면 게이트가 붉어질 것.

### P2-C. 표본 23건 중 20건이 `처분방식 확인` 으로 떨어져 코치가 임대 분기를 탄다 — **범위 밖 기록**

- `disposition` 은 `dspsMthodNm` hidden input → `op_cm_badge` 텍스트 → 공공데이터 API 순으로 찾는데, 저장 HTML 23건에서 앞 둘이 모두 빈 값이다 `[관측]`. API 키가 없으면 세 번째도 없다.
- 결과로 압류재산 3건 외 20건이 `is_sale=False` → 「AI 누락 점검: 임대 후 실제 사용 조건」 분기를 탄다.
- **이번 diff 는 이 로직을 건드리지 않았다** `[관측]` — 범위 밖이므로 판정하지 않고 기록만 남긴다. ①의 화면 노출 수치가 3건뿐인 이유이기도 하다.

---

## ⑺ 재현하지 못한 것과 이유

| 항목 | 이유 |
|---|---|
| `~/onbid-survey-backup/html/` 경로의 표본 | 디렉터리가 0건이다 `[관측]`. `/tmp/onbid-survey/html/` 23건으로 대체했다 |
| 라이브 온비드 페이지에서의 `is_sale` 분포 | 저장 HTML 로만 측정했다. 라이브에서는 `dspsMthodNm`·배지·API 가 채워져 `is_sale=True` 공고가 더 많을 수 있다 `[추론]` — 그러면 ①-c 의 「화면 노출 위양성 0건」도 커질 수 있다 |
| 2차 세션의 「자작 변이 2개도 rc=1」(`handoff.md:21`) | 그 변이 목록이 레포에 산출물로 없다. 종료 조건 4개만 내 변이로 대체 검증했다 |
| 공공데이터 API 6종 | `ONBID_API_SERVICE_KEY` 부재. 이번 diff 가 `PUBLIC_DATA_ENDPOINTS` 를 건드리지 않았음만 확인했다 `[관측]` |
| 실 URL e2e `3 passed` | 이번 재판정에서는 `screen.spec.js` 2/2 만 돌렸다. 실 URL 스펙은 네트워크 의존이라 재실행하지 않았다 |
| 첨부 파일 내부(PDF·HWP) | 범위 밖 · to-do 미결 |

---

## ⑻ 검증 경계

| 구분 | 상태 |
|---|---|
| 순수 함수 게이트 | ✅ 114/114 재현 · 자작 변이 4/4 rc≠0 · 비등가 3/4 확인 |
| 화면 노출 게이트 | ✅ 2/2 재현 · PLAYED·AXES 출력 확인 |
| 표본 23건 계측 | ✅ 10/23 · 59/59 · 20/20 재현 |
| `has_direct_submit` 세 안 분해 | ✅ 2/23 · 10/23 · 16/23 실측 · 화면 출력 직접 관측 |
| 회의록 근거 대조 | ✅ `11_캠코_회의록_20260907.md:265` 원문 확인 |
| 라이브 온비드 경로 | 🔴 미검증 — 저장 HTML 로만 측정 |
| 공공데이터 API 6종 | 🔴 인증키 부재로 미검증 |
| 실 URL e2e | 🔴 이번 회차 미실행 |
| 열거 대조 | 조건 축 코드 **10** = `server.py:252` **10** = `changelog.md:46` **10** — 일치 |
| 수치 전파 | 🔴 `changelog.md:53` 「99단언 · 킬러 변이 39개」 미갱신(P2-A) |
| 워킹트리 | ✅ 재판정 전 구간 `git status --porcelain` **빈 출력**. 변이·계측 파일은 전부 `/tmp` 사본 |

---

## ⑼ 최종 판정

**조건부 GO — push 해도 된다.**

- P0 없음. 코드 회귀·거짓 안내·보안 문제는 확인되지 않았다.
- 1차 리뷰의 종료 조건 4개는 닫혔고 헤드라인 지표는 전부 재현된다.
- 조건 2개는 **문서·주석 정정과 판단 기록**이며 코드 되돌림을 요구하지 않는다.
  1. **P1-A** — `has_direct_submit` 주석의 「표본 6건 실측」 귀속 정정(그 6건은 화면에 뜨지 않았다).
  2. **P2-A** — `changelog.md:53` 의 「99단언 · 킬러 변이 39개 전부 붉어짐」 갱신.
- **P1-B**(중간안 채택)는 동원님 판단 사항이며 push 를 막지 않는다. 다만 표본 기준으로는 **중간안이 현행보다 낫다** — 위양성을 늘리지 않으면서 압류재산 3건의 안내를 되살린다.
