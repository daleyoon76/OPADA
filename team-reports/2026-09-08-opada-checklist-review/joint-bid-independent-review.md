# 공동입찰 축 물림 — 독립 리뷰 (재현 기반)

- 판정 대상 커밋: `0db865d` (push 후보) · diff 범위 `62279f8..0db865d` · 커밋 3건 `7f6aafd`·`9161fb8`·`0db865d`
- 기준 시각: 2026-09-08 05:09 KST(`date`) · 작성 시각: 2026-09-08 05:15 KST(`date`)
- 리뷰 성격: 리뷰형 소집(구현 세션과 저자 분리) · 산출물 이 파일 1개 · 레포 코드/문서 무수정
- 실험 격리: `/tmp/opada-rev`(레포 `cp -R` 사본)에서만 변이 실행. 레포 워킹트리는 `git status --porcelain` 빈 출력 확인

## 최종 판정 — 조건부 GO

- **코드 3커밋(`7f6aafd`·`9161fb8`)은 GO.** 구현 세션의 코드 관련 주장 8건을 전부 독립 재현했다.
- **문서 커밋 `0db865d` 에 P1 과대주장 1건**이 있다. 그 문장의 범위를 좁히면 push 해도 된다.
- P1 을 고치지 않고 push 해도 코드는 안전하다. 다만 그 문장은 커밋 메시지 제목(「itemDetail 개통 확인」)에도 실려 있어 완료 보고로 읽힌다.

## 1. 구현 세션 주장 재현표

| # | 주장 | 판정 | 근거 |
|---|---|---|---|
| 1 | `joint` 9/23 → 12/23 · 증분 17·18·21 · 반대 불일치 0/23 | ✅ 재현 | `[관측]` §2-1 |
| 2 | 공동입찰 못 읽음 0/23 · 어휘 2개 | ✅ 재현 | `[관측]` §2-1 |
| 3 | 순수 함수 141/141 · 축 19 · 미커버 0 | ✅ 재현 | `[관측]` §2-2 |
| 4 | 화면 2 passed · 축 9 · 미커버 0 | ✅ 재현 | `[관측]` §2-2 |
| 5 | 킬러 변이 5건 rc≠0 · 툴팁 오탐 가드 유효 | ✅ 재현(자작 변이 14건) | `[관측]` §3 |
| 6 | 제출서류 10/23(8/8·2/13·0/2) · 첨부 20공고 59/59 무변동 | ✅ 재현 | `[관측]` §2-1 |
| 7 | `or` 확장이 기존 근거를 덮지 않음 | ✅ 재현 | `[관측]` §3·§5 |
| 8 | `itemDetail` 개통 · 필수 파라미터 4개(`cltrHisNo` 불필요) | ⚠️ **부분 재현** | `[관측]` §6 — 물건상세 입력에서만 참 |
| 9 | 명도책임 조건 축 아님 · `매수인` 3/23 | ✅ 재현 | `[관측]` §2-1 |

## 2. 게이트 재실행 (전부 내가 직접 실행)

### 2-1. 표본 23건

`ONBID_SAMPLE_HTML_DIR=/tmp/onbid-survey/html python3 tests/measure_sample_23.py` → rc=0 `[관측]`

```
구체 서류명 1건 이상 추출: 10/23   A형: 8/8 · B형: 2/13 · C형: 0/2
첨부 다운로드 파라미터: 59/59 (첨부가 있는 공고 20건)
joint 서류 근거: 9/23 [1,2,3,4,5,6,7,8,20]
joint 화면 값 「가능」: 12/23 [1,2,3,4,5,6,7,8,17,18,20,21]
joint 합산(현행 판정): 12/23 · 증분 [17,18,21]
joint 반대 방향(서류 켜짐 · 화면 「불가능」): 0/23 []
공동입찰 값 못 읽음: 0/23 []
명도책임 값: 3/23 [1,2,3] · 어휘 ['매수인']
```

어휘를 별도로 셌다 `[관측]` — 공동입찰 `{가능:12, 불가능:11}` · 대리입찰 `{가능:12, 불가능:11}`. 제3의 어휘 0건.

### 2-2. 게이트 2종

- `python3 tests/test_docs_extraction.py` → `141/141 통과 · 연기 3개 · 축 19개 중 미커버 0개` `[관측]`
- `npx playwright test tests/screen.spec.js` → `2 passed (2.5s)` · `연기 2개 · 축 9개 중 미커버 0개` `[관측]`

## 3. 자작 변이 14건 (구현 세션 스크립트 미사용)

`/tmp/opada-rev` 사본에 직접 앵커를 짜 넣어 돌렸다. 각 변이마다 순수 함수 게이트 rc 와 표본 수치 변화를 함께 잰다.
macOS 시스템 python3.9 는 바이트코드를 `~/Library/Caches/com.apple.python/` 에 쓴다 — 같은 바이트 수 변이는 캐시가 낡는다. 캐시를 지우고 `PYTHONDONTWRITEBYTECODE=1` 로 재실행한 결과다 `[관측]`.

| 변이 | 내용 | 게이트 | 표본 |
|---|---|---|---|
| R1 | `tit_area01` 창 경계 삭제 | ❌ rc=1 (B8 음성 값 없음) | 동일 |
| R2 | 절 전체를 훑어 첫 `ico_box01` 값 | ❌ rc=1 (3 FAIL · 툴팁 포함) | 동일 |
| R3 | 라벨 문자열 존재만 보고 `가능` | ❌ rc=1 (3 FAIL) | **23/23 로 폭발** |
| R4 | 값 정규식에서 `ico_box01` 앵커 삭제 | ⭕ **생존** | 동일 |
| R5 | 빈 값 가드(`if value:`) 삭제 | ⭕ **생존** | 동일 |
| R6 | 첫 라벨 일치에서 즉시 반환 | ⭕ **생존** | 동일 |
| R7 | `or` → 화면 값으로 덮어쓰기 | ❌ rc=1 (B9 기존 근거 보존) | 동일 |
| R8 | 화면 값 무시(옛 동작) | ❌ rc=1 (B9 양성) | 동일 |
| R9 | 값이 있기만 하면 켜기 | ❌ rc=1 (B9 음성 불가능) | 동일 |
| R10 | 문구 분기를 항상 옛 문장으로 | ❌ rc=1 | 동일 |
| R11 | 명도책임 기본값 `매수인` | ❌ rc=1 | **23/23 로 폭발** |
| R12 | 명도책임 정규식을 `.*?` 로 느슨하게 | ⭕ **생존** | 동일 |
| R13 | `build_notice` 의 `jointBidAllowed` 를 `""` 로 | ⭕ **생존** | 동일 |
| R14 | `build_notice` 가 `대리입찰` 값을 싣도록 | ⭕ **생존** | 동일 |

- **주장 5 는 성립한다** `[관측]`. 툴팁 오탐 경로(R2·R3)는 내가 새로 설계한 변이로도 붉어진다. R3 은 표본 화면 값을 23/23 으로 부풀려 실제 위험이 크다는 것도 같이 보인다.
- 생존 6건 중 R4·R5·R6·R12 는 **방어적 과잉 명세**다(현 표본에서 결과를 바꾸지 않음). R13·R14 는 성격이 다르다 — §4 참조.

## 4. 창 경계(`tit_area01`)가 실제로 필요한가 — 필요하다

표본 23건의 「입찰방법」 절은 라벨 16개 / 값 박스 10개다 `[관측]`. 값 박스가 없는 항목이 실재한다.

경계 가드를 뺀 사본과 대조한 결과 `[관측]`:

- 값이 달라지는 (공고, 라벨) 쌍 **46건** = `입찰보증금`·`잔대금 납부방법` × 23건. 가드 없으면 둘 다 옆 항목의 `가능` 을 훔쳐온다.
- 다만 **실제로 질의하는 두 라벨(`공동입찰`·`대리입찰`)에서는 23/23 모두 값이 자기 박스에 있어 가드가 결과를 바꾸지 않는다.**

판정: 가드는 **무증명이 아니다**(도둑질 경로가 표본에 실재하고 픽스처 B8 이 그것을 붉힌다). 다만 현재 제품 경로에서는 하중을 받지 않는다.

부수 관찰 `[관측]`: 같은 절에서 `잔대금 납부기한` 을 질의하면 `가능` 이 나온다(날짜여야 할 항목). 제품 코드가 이 라벨을 부르지 않아 지금은 무해하지만, 라벨을 늘릴 때 재검증이 필요하다.

## 5. `or` 확장이 순수 증분인가 — 그렇다

- 값이 `불가능` 인 11건 = 표본 9·10·11·12·13·14·15·16·19·22·23 `[관측]`.
- 이 11건 중 **서류 근거(`docJoint`)가 켜진 것은 0건**이다. 서류 근거 목록 `[1..8,20]` 과 교집합이 비어 있다 `[관측]`.
- 따라서 「기존 켜짐을 끄는」 사례는 구조적으로 존재하지 않는다. 반대 방향 0/23 은 측정 결과이자 이 집합 관계의 귀결이다.
- 변이 R7(덮어쓰기)·R8(무시)이 각각 붉어져 `or` 의 두 방향이 모두 단정돼 있다 `[관측]`.

## 6. 🔴 주장 8 — 부분 재현 (새 지적 P1)

라이브 실측 `[관측]`, 2026-09-08 05:14~05:15 KST:

| 입력 유형 | `pbctCdtnNo` | `build_related_urls()` 의 `itemDetail` |
|---|---|---|
| 물건상세 URL(시연용 3건 중 1건) | 있음(`5988361`) | **HTTP 200 · 724,351 bytes** |
| 공고상세 URL(표본 #1) | 없음 | **HTTP 500** |
| 공고상세 URL(표본 #17) | 없음 | **HTTP 500** |

- 표본 23건은 전부 공고상세 페이지이고, **hidden input 에 `pbctCdtnNo` 가 있는 것은 0/23** 이다 `[관측]`.
- `build_related_urls()` 는 빈 값을 URL 에서 떨어뜨리므로, 공고상세를 붙여넣은 사용자에게 나가는 `itemDetail` 링크에는 `pbctCdtnNo` 가 없다 → 500.
- 500 은 구현 세션이 스스로 적은 「나머지 4개는 각각 빼면 500」과 정확히 일치한다 `[추론]`. 즉 코드는 문서대로 동작하고, **틀린 것은 문서의 범위**다.

`handoff.md` 의 문장 「`build_related_urls()` 의 `itemDetail` GET URL 은 라이브에서 HTTP 200·667KB 로 열립니다. **죽은 링크가 아닙니다**」와 커밋 제목 「itemDetail 개통 확인」은 **물건상세 입력 1건에서만 참**이다. 공고상세 입력에서는 2/2 실측 500 이다.

## 7. 증분 3건이 제품으로서 맞는가 — 2건은 맞고, 1건은 화면에 안 나온다

로컬 코드로 실제 문구를 뽑았다(`build_notice` + `local_ai_coach`, 표본 HTML 주입, 재산유형·처분방식은 `samples.json` 관측값) `[관측]`.

- **#17(기타일반재산 / 매각)**·**#18(파산자산 / 매각)** — 공동/대리 항목이 뜬다.
  - 바로 위 줄: 「서류 목록을 자동으로 뽑지 못했습니다」
  - 공동/대리 줄: 「온비드 「입찰방법」에 공동입찰이 `가능`으로 표시돼 있습니다. 서류 목록은 원문에서 뽑지 못했습니다.」 · 출처 `온비드 입찰방법`
  - **판정: 제품으로서 맞다.** 없는 서류 목록을 전제하지 않고, 근거 출처가 화면 값임을 그대로 밝힌다. 두 줄이 서로 모순되지 않는다.
- **#21(기타일반재산 / 임대)** — 공동/대리 항목이 **뜨지 않는다.** 코치 블록 전체가 `is_sale` 게이트 안에 있고 #21 은 임대이기 때문이다.
  - 화면에 남는 변화는 quick-facts 의 `공동입찰: 가능` 행 하나뿐이다.
  - **「증분 3건」은 조건 축 기준으로는 맞고, 사용자에게 보이는 코치 변화 기준으로는 2건이다.**

「의도대로 동작하는가」 → 예. 「이 상태가 제품으로서 맞는가」 → 예(2건). 다만 문구 정합에 P2 1건(§8-3).

## 8. 새 지적

### P1-A · `itemDetail 개통` 주장이 코드보다 넓다

- 위치: `handoff.md`(0db865d 신설 블록) · 커밋 `0db865d` 제목 · `to-do.md` 발견 사항
- 사실: 물건상세 입력 200 / 공고상세 입력 500(2/2) · 표본의 `pbctCdtnNo` 보유율 0/23 `[관측]`
- 왜 P1: 「죽은 링크가 아니다」는 탐색을 **닫는** 판정이고, 사용자 입력의 다수 경로(공고상세)에서 거짓이다.
- **최종 커밋에서 확인할 종료 조건 1개**: `handoff.md`·`to-do.md` 에서 `itemDetail` 을 언급한 줄을 grep 해, 모든 줄이 「물건상세 입력 기준」또는 「`pbctCdtnNo` 가 있을 때」라는 조건을 달고 있을 것(조건 없는 줄 0건).

### P2-B · `build_notice` 의 `jointBidAllowed` 배선에 단정이 없다

- 사실: 변이 R13(`""` 로 고정)·R14(`대리입찰` 값을 실음)이 **순수 함수 게이트와 표본 스크립트 양쪽에서 생존** `[관측]`.
- 원인: `test_docs_extraction.py` 는 `build_notice` 를 PLAYED 로 선언해 부르지 않고, `measure_sample_23.py` 는 `bid_method_flag()` 를 직접 부른다. 두 게이트 모두 호출부를 지나치지 않는다.
- 표본 #17(공동 `가능`/대리 `불가능`)·#22(공동 `불가능`/대리 `가능`)가 R14 를 가르는 판별 케이스인데 아무도 보지 않는다.
- 배선 자체는 내가 런타임으로 확인했다 `[관측]` — 표본 17·18·21·22·1 에서 `build_notice()['jointBidAllowed']` 가 `bid_method_flag(split_sections(html),'공동입찰')` 과 5/5 일치. 그래서 **결함이 아니라 게이트 공백**이다(전역 규칙 [S-5] 의 「호출부 배선 assert」 미충족).
- **종료 조건 1개**: `measure_sample_23.py` 의 `screenJoint` 를 `bid_method_flag` 직접 호출이 아니라 `build_notice` 산출값으로 바꿨을 때 표본 #17·#22 값이 각각 `가능`·`불가능` 일 것.

### P2-C · #21 형 공고에서 quick-facts 행만 남는다

- 사실: 증분 3건 중 #21(임대)은 코치 항목이 `is_sale` 게이트에 막혀 나오지 않는다 `[관측]`.
- 문서·커밋 메시지의 「증분 3건」은 축 기준 수치이며 화면 기준으로는 2건이다. 수치 자체가 틀린 것은 아니나 근거 단위가 적혀 있지 않다.
- **종료 조건 1개**: 「증분 3건」을 적은 모든 위치에 「조건 축 기준(화면 코치 노출은 2건 — #21 은 임대라 `is_sale` 게이트에 걸림)」이 병기돼 있을 것.

### P2-D · 코치 action 문구가 서류 0건 공고와 어긋난다

- 사실: #17·#18 에서 reason 은 「서류 목록은 원문에서 뽑지 못했습니다」인데 같은 항목의 action 은 「… 필요한 서류만 남깁니다」다 `[관측]`. 남길 목록이 화면에 없다.
- reason 분기는 새로 갈랐으나 action 은 옛 문장 그대로다.
- **종료 조건 1개**: `has_doc_condition` 이 거짓인 경로의 `action` 이 「남깁니다」류 목록 전제 동사를 쓰지 않을 것.

### P2-E · 방어 코드 4곳이 어느 게이트에도 걸리지 않는다

- R4(`ico_box01` 앵커) · R5(빈 값 가드) · R6(라벨 반복 순회) · R12(명도책임 정규식 밀착)이 모두 생존 `[관측]`.
- 온비드 실제 마크업이 라벨을 2벌(16개 = 8×2) 싣기 때문에 R5·R6 경로는 표본에서 발동하지 않는다.
- 결함은 아니다. 「선언이 코드보다 넓게 주장하지 않는다」 기준으로, 이 4곳은 주석에 「현 표본에서는 발동하지 않는 방어」로 표기하는 편이 낫다.
- **종료 조건 1개**: `bid_method_flag()` 주석에 「표본 23건에서 이 가드가 결과를 바꾸는 라벨은 `공동입찰`·`대리입찰` 이 아니다」가 적혀 있을 것.

## 9. 미검증 (이유 포함)

| 항목 | 이유 |
|---|---|
| `cltrHisNo` 를 뺀 5회 파라미터 소거 실험 재현 | 시간 예산. 대신 「4개를 다 실은 URL 이 200」을 1건 실측해 결론만 대조했다 |
| 시연용 실URL 3건 중 나머지 2건의 `itemDetail` | 1건(200)만 실측. 나머지는 미확인 |
| `build_notice` 의 `assetType`·`dispositionLabel` 추출 | 저장 HTML 에서 `중메뉴 펼치기` 가 나온다. 이번 diff 밖이고 `measure_sample_23.py` 도 `samples.json` 관측값을 쓴다 |
| 로컬 서버 `/api/analyze` 실호출 | 라이브 온비드 fetch 가 필요해 표본 재현성이 없다. 함수 수준 런타임 관찰로 대체 |
| 명도책임 물건상세 경로 | 구현 미착수 범위 |

## 10. 검증 경계

| 축 | 결과 |
|---|---|
| 순수 함수 | ✅ 141/141 · 축 19 · 미커버 0 (내가 실행) |
| 화면 | ✅ 2 passed · 축 9 · 미커버 0 (내가 실행) |
| 표본 23건 | ✅ rc=0 (내가 실행) |
| 자작 변이 | ✅ 14건 · 붉힘 8 / 생존 6 |
| 호출부 배선 | ⚠️ 게이트 없음 · 리뷰어 런타임 관찰 5/5 일치로 대체 |
| 라이브 네트워크 | ✅ 3 URL 실측(200 1건 · 500 2건) |
| 레포 워킹트리 | ✅ `git status --porcelain` 빈 출력(이 보고서 추가 전) |
| 리뷰어 자작 종료 조건 | ✅ 5건(P1-A · P2-B · P2-C · P2-D · P2-E) |
