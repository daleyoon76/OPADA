# researcher 반환물 — 준비서류 추출 알고리듬 실태·gap·설계안

- 소집: 2026-09-09 3팀 UIUX품질팀 (진단형 + 재기획) · 알고리듬 축으로 1팀 researcher 를 병렬 투입
- 역할: researcher (읽기 전용)
- 반환 시각: 2026-09-09 12:16 KST
- 팀장 주: 원문 그대로 보존. 검증·채택 판단은 종합 문서에서 별도로 한다.
- 검증 상태: **미검증 (팀장 재대조 전)**

---

기준 시각: 2026-09-09 12:0x KST (측정 스크립트 실행 시각) · 대상 커밋 `065a3ac`
범위: 읽기 전용. 파일 수정 0건. 실행한 것은 계측 스크립트 1개와 `python3 -c` 계수 3회뿐입니다.

---

## 1. as-is 데이터 흐름

### 1-1. 파이프라인 (입력 → 출력)

| 단계 | 함수 / 상수 | 위치 | 하는 일 · 거르는 것 |
|---|---|---|---|
| ① 페이지 수집 | `fetch_onbid()` | `src/server.py:698` | `validate_onbid_url()`(`:676`)이 host 를 `onbid.co.kr` 로 제한. 첨부·외부 URL 은 이 관문을 통과하지 못함 |
| ② 절 분해 | `split_sections()` | `src/server.py:113` | `h2.tit` 로 절을 자르고 **같은 제목 조각을 이어붙임**(PC/모바일 이중 마크업 대응, `:121`) |
| ③ 탐색 절 한정 | `DOC_SEARCH_SECTIONS = ("공고문", "제출서류")` | `src/server.py:131` | 「입찰방법」 절을 **탐색 대상에서 제외** — 가드 ① |
| ④ 툴팁 제거 | `strip_tooltips()` / `TOOLTIP_BLOCK_RE` | `src/server.py:135` · `:132` | `div.tooltip_wrap01` 블록 삭제 — 가드 ② |
| ⑤ 줄 단위화 | `block_text()` | `src/server.py:87` | 닫는 태그를 개행으로 바꿔 **항목 경계 보존**. `clean_text()`(`:41`)를 쓰면 셀 경계가 소실되므로 별도 함수 |
| ⑥ 줄 필터 | `extract_doc_items()` 루프 | `src/server.py:417-420` | 빈 줄 제외 · **1,200자 초과 줄 제외** |
| ⑦ 조건 추출 | `find_conditions()` · `CONDITION_AXES` | `src/server.py:364` · `:312` | 축 **10개**. 같은 줄에 `CONDITION_NEGATIVE_RE`(`:327`)가 걸리면 `negative=1` 로 표시하고 `:421` 에서 **버림**(표본 19 「불허용」 대응) |
| ⑧ 서류명 추출 | `find_doc_names()` · `DOC_NAME_PATTERNS` | `src/server.py:352` · `:260` | 닫힌 어휘. 매칭 구간을 `\x00` 으로 **마스킹**(`:360`)해 `법인 인감증명서`→`인감증명서` 중복 계상을 차단 |
| ⑨ 조건 물림 | `carried` / `LINE_RESET_RE` · `SUB_ITEM_RE` | `src/server.py:416`·`:426`·`:429` | 머리글의 조건을 하위 불릿에 물림. 새 상위 번호가 조건 없이 오면 끊음 |
| ⑩ 단순 언급 배제 | `DOC_REQUEST_RE` · `authorizing` | `src/server.py:302` · `:433-437` | 제출 동사도 조건 문맥도 없으면 항목으로 세지 않음. **`license` 축은 authorizing 에서 제외** |
| ⑪ 부가조건 | `doc_extras()` | `src/server.py:373` | 원본/사본 · 유효기간 · 제출방법 · 기한 · 부수. 없으면 **빈 문자열이며 지어내지 않음** |
| ⑫ 단계 판정 | `CONTRACT_STAGE_RE` | `src/server.py:306` · `:439` | 「입찰 전」 / 「낙찰 후·계약 시」 |
| ⑬ 표 파싱(별도) | `extract_docs_table()` · `GENERIC_DOC_NAME_RE` | `src/server.py:472` · `:469` | 온비드 「제출서류」 표 행을 `generic` 플래그로 갈라 저장. **`items` 에 합치지 않음** |
| ⑭ 조립 | `build_doc_checklist()` | `src/server.py:501` | 상태 4종 — `extracted` / `not_in_notice`(C형) / `attachment_only` / `not_found` (`:517-528`) |
| ⑮ 사전 판정 | `doc_source_profile()` · `DOC_SOURCE_BY_ASSET_TYPE` | `src/server.py:250` · `:217` | 재산유형 → A/B/C/unknown |
| ⑯ 첨부 | `extract_related_docs()` | `src/server.py:564` | `fn_chkPdfRead(...)` 5인자에서 3개를 뽑아 **다운로드 URL 생성**(`:580`) |
| ⑰ 배선 | `build_notice()` | `src/server.py:1738-1740`, `:1782` | `sections` → `build_doc_checklist` → `docChecklist` 와 `required_docs` |

### 1-2. 닫힌 어휘 목록 — 실측 계수 (오늘 실행)

- `DOC_NAME_PATTERNS` = **36 항목**(`src/server.py:260-298`) `[관측]`
  - 라벨이 있는 **닫힌 서류명 34개** + 라벨이 빈 **열린 패턴 2개**(`[가-힣]{2,10}\s*등록증` · `…면허증`, `:296-297`)
- `CONDITION_AXES` = **10개**(`src/server.py:312`) `[관측]`
- `DOC_HOWTO`(발급처 안내) = **17개**(`src/server.py:330`) — 34개 서류명 중 17개만 안내 문장을 가짐 `[관측]`
- `DOC_SOURCE_BY_ASSET_TYPE` = **6개**(`src/server.py:217`) `[관측]`

### 1-3. 오늘 새로 관측한 것 — 팀 보고서 5건에 기록이 없는 사항

- 🔴 **LLM 경로는 응답 내용을 화면에 쓰지 않습니다** `[관측]`
  - `gemini_ai_coach()` 는 응답을 `parse_gemini_json()` 으로 파싱해 **성공 여부만 판정**하고(`src/server.py:1442-1446`), 반환값은 `sanitize_ai_text(local_ai_coach(...))` 입니다(`:1447-1452`).
  - `vertex_ai_coach()` 도 동일 구조입니다(`src/server.py:1525-1535`).
  - 즉 LLM 이 붙으면 바뀌는 것은 `mode`·`llmStatus`·`model` 라벨뿐이고, 항목 내용은 규칙 기반 산출물입니다.
  - `--include="*.md"` 전수 grep 에서 이 사실을 적은 문서 0건 `[관측]`.
- **런타임은 첨부를 열지 않습니다** `[관측]` — `src/server.py` 의 `urlopen` 호출은 온비드 페이지·공공데이터 API·LLM 3곳뿐입니다. 첨부 URL 은 `src/app.js:1050` 의 「내려받기」 링크로 **사용자에게 넘깁니다**.

---

## 2. 표본 23건 대비 커버리지 판정표

### 2-1. 오늘 실행한 계측 (`python3 tests/measure_sample_23.py`, rc=0, 표본 23/23 존재)

| 유형 | 표본 | 「구체 서류명 1건 이상」 | 상태값 | 판정 |
|---|---|---|---|---|
| **A형** | 8 | **8/8** | 전건 `extracted` | 본문 경로 커버 `[관측]` |
| **B형** | 13 | **2/13** (#14 · #20) | 나머지 11건 `attachment_only` | **첨부 본문 미커버** `[관측]` |
| **C형** | 2 | **0/2** | 전건 `not_in_notice` | 정답이 「없음」이므로 맞는 값 `[관측]` |
| 합계 | 23 | **10/23** | — | `handoff.md:251`·`to-do.md:121` 기재와 오늘 재현 일치 |

부수 지표(같은 실행) `[관측]`: 첨부 다운로드 파라미터 **59/59 파일**(첨부 보유 20건) · `joint` 축 9/23 → 화면 12/23 · 반대 방향 불일치 0/23 · `build_notice` 배선 일치 **23/23** · 명도책임 3/23

### 2-2. B형 13건 — 현재 코드가 할 수 있는 것 / 없는 것

| 항목 | 판정 | 근거 |
|---|---|---|
| 첨부 **목록·파일명** 추출 | ✅ 가능 | `extract_related_docs()` `src/server.py:564` · 오늘 실측 59/59 |
| 첨부 **다운로드 URL 생성** | ✅ 가능 | `src/server.py:580` · 오늘 실측 59/59 |
| 첨부 **파일 내려받기(서버측)** | ❌ 못 함 | 첨부 대상 HTTP 호출 0곳 `[관측]` |
| 첨부 **텍스트 추출** | ❌ 못 함 | 외부 의존성 0. 오늘 확인 — `pdftotext`·`hwp5txt`·`libreoffice` 전부 `not found`, 시스템 python3.9 에 `pdfminer` 없음 `[관측]` |
| B형 사용자에게 주는 것 | 「본문에서 못 찾음 + 첨부 확인」 안내 + 내려받기 링크 | `src/server.py:523-525` · `src/app.js:1050` |
| B형 13건 중 본문에서 뽑히는 2건 | #14 · #20 | 오늘 실행 출력 |

### 2-3. 「A형 8/8」이 감추는 것 — 지표 성격 주의

- 현재 지표는 **공고당 「1건 이상 뽑았는가」의 이진값**입니다(`tests/measure_sample_23.py:133`) `[관측]`.
- 항목 단위 재현율·정밀도는 **어디에서도 계산하지 않습니다** `[관측]`.
- 국유재산 5건(#04~08)의 오늘 출력 조건은 `공동입찰 · 대리입찰`뿐인데, 표본조사는 「국유재산 5건은 미성년자 조건이 본문에 없고 첨부 PDF 에만 있다」고 적었습니다(`docs/90_MVP개발/07_제출서류_표본조사_20260907.md:77-78`) — **A형 8/8 안에도 첨부에만 있는 누락이 있습니다** `[관측 · 두 산출물 대조]`.

### 2-4. 어휘 목록의 양방향 오차 — 오늘 실측한 반례 1건

표본조사가 인용한 [13] 첨부 원문 1줄(`07_제출서류_표본조사:90`)에 `find_doc_names()` 를 돌린 결과 `[관측]`:

- **누락** — 「공유재산 사용허가 신청서」가 결과에 없습니다. `DOC_NAME_PATTERNS` 에 `사용허가 신청서` 계열이 없습니다.
- **과다** — 「주민등록증」·「운전면허증」이 **독립 서류로 잡힙니다**. 원문에서는 `신분증(주민등록증, 운전면허증 또는 여권)` 의 예시입니다. 열린 패턴(`:296-297`)이 원인입니다.
- 이 두 오차는 「1건 이상」 지표로는 **둘 다 보이지 않습니다**.

---

## 3. 정답 세트 존재 여부와 측정 실태

### 3-1. 정답 세트

- **존재하지 않습니다** `[관측]`
  - 파일명 탐색 0건. 내용 탐색 결과 제출서류 관련 언급은 전부 **작성 계획**입니다(`07_표본조사:286`·`:305`, `12_제품방향:135`, `to-do.md:130` 미체크).
  - `docs/90_MVP개발/source-notes/onbid_제출서류_표본_20260907.md`(385줄)는 **원문 발췌 모음**이지 항목화된 정답표가 아닙니다 `[관측]`.

### 3-2. 「A형 8건 정답 대비 몇 %」 질문에 대한 답

- **미측정입니다.** 정답 세트가 없어 분모가 없습니다 `[관측]`.
- 인용 가능한 가장 가까운 수치는 **「공고당 1건 이상 추출 = A형 8/8」**뿐이며, 항목 단위 정확도가 아닙니다.
- 비공식 대조 1건 `[관측 · 정답 대비 측정이 아님]` — 표본조사는 [01] 본문에 구체 서류명 **11종**이 있다고 적었고(`07_표본조사:250`), 오늘 실행에서 #01 고유 서류명은 **12개**로 그 11종을 포함하고 `법인인감증명서` 1개가 더 잡혔습니다. **이 11종은 검수된 정답이 아니므로 정확도 수치로 승격시키면 안 됩니다.**

### 3-3. 지금 무엇을 재고 있는가

| 도구 | 무엇을 재는가 |
|---|---|
| `tests/measure_sample_23.py` | 계측 전용. 공고당 「1건 이상」 · 첨부 URL 수 · joint 축 · 배선 일치 · 명도책임 |
| `tests/test_docs_extraction.py` | 게이트. `PLAYED`·`AXES` 를 실행마다 출력 |
| 두 가드 분리 검사 | 절 배제와 툴팁 제거를 **각각** 붉히는 픽스처 존재 (`:168-175`, A1 `:253`) |
| 부정 문맥 | A4 축 「불허용」(`:45`·`:363`) |
| 구분명 표 | A9·B1(`:469`) |

- 두 도구 어디에도 **항목 단위 정답 대조가 없습니다** `[관측]`.
- `measure_sample_23.py` 는 표본 HTML(`/tmp/onbid-survey/html/`)이 있어야 돌며, 이 원자료는 레포에 없습니다 — **휘발 위험이 있는 측정 기반**입니다.

---

## 4. 설계 선택지 3갈래

### 갈래 1 — 규칙 기반 확장 (현행 연장)

| 축 | 내용 |
|---|---|
| 커버 범위 | A형 8/8 유지 + B형은 **첨부 파서 도입 시에만** 열림 |
| 필요한 것(파서 미도입) | 어휘 확장 · 절 제목 8종 대응 · 열린 패턴 정밀화. B형 13건은 계속 `attachment_only` |
| 필요한 것(파서 도입) | PDF·HWP·HWPX 3계열 파서 + 매직바이트 분기 + HWP 표 복구(`hwp5html`) |
| 비용(미도입) | 낮음. 상수·정규식 수정 범위 |
| 비용(도입) | 외부 의존성 3~4개 신규. 「표준 라이브러리만」 성질이 깨짐 |
| 정확도 측정 | 정답 10건 × 항목 단위 precision/recall. `measure_sample_23.py` 에 항목 대조 함수를 추가하되 **기존 「1건 이상」 지표와 갈라서** 출력 |
| 위험 | ① 닫힌 어휘 양방향 오차(§2-4) ② 표를 텍스트로 펴면 셀 경계 소실 ③ 절 제목이 8가지 ④ 목록이 몇 번째 첨부인지 파일명으로 못 고름 |

### 갈래 2 — 언어 모델 단독

| 축 | 내용 |
|---|---|
| 커버 범위 | 이론상 A·B 전체. 단 **입력이 텍스트여야** 하므로 첨부 파서 문제는 그대로 남음 |
| 필요한 것 | ⑴ 첨부 텍스트화(갈래 1과 동일 비용) ⑵ 실제 LLM 출력을 쓰는 경로 — **현행 코드는 LLM 출력을 버립니다**(`:1447-1452`·`:1530-1535`) ⑶ 스키마 강제 + 사후 검증 |
| 비용 | 파서 + 호출 + 검증 코드. 공개 ngrok 서버에 인증이 없어 키 관리 문제도 생김 |
| 정확도 측정 | 항목 단위 대조 + **환각 지표를 따로 셈** |
| 위험 | ① **원문에 없는 서류명 생성** — C형 2건이 시험대 ② 근거(`evidence`) 소실 — 현행은 항목마다 원문 300자를 붙임(`:461`) ③ 재현성 없음 ④ 가드레일 |

### 갈래 3 — 혼합 (규칙이 후보 · 모델이 보완 · 규칙으로 검증)

| 축 | 내용 |
|---|---|
| 커버 범위 | A형은 규칙(결정적·근거 보유), B형 첨부는 모델, 최종 항목은 **원문 부분문자열 대조를 통과한 것만** |
| 비용 | 가장 큼(세 부품 전부) |
| 정확도 측정 | 3열 비교 — 규칙 / 모델 / 혼합. 혼합은 「검증기가 버린 항목 수」도 출력 |
| 위험 | 부품이 셋이라 회귀 축이 늘고 실패 귀속이 어려움 |

### 4-4. 언어 모델 경로와 기존 가드레일의 충돌 경계

| 행위 | 판정 | 근거 |
|---|---|---|
| 첨부 원문에서 **서류명·기한·부수를 뽑아 나열** | 금지 문언에 해당하지 않음 `[추론]` | 금지 목록은 「판단」 4종이고 추출은 그 목록에 없음 (`CLAUDE.md:31`) |
| 「이 서류를 갖췄으므로 자격이 됩니다」 | **금지** `[인용]` | 밸리데이션 금지(`12_제품방향:103-105`) |
| 원문에 없는 서류를 추정해 추가 | **금지에 준함** `[추론]` | 「사실만 말하라 · 추측하지 마라」(`CLAUDE.md:28-30`) |
| 낙찰 가능성·수익성·권리분석 문장 | **금지** `[인용]` | 금지표현 12종 |
| 발표자료에 「Gemini AI/Vertex AI」 표기 | **금지** — 발표·시연·외부 공유 문서 한정 `[인용]` | 코드에서 그 API 를 쓰는 것 자체를 금지한 문장은 아님 |

- 🔴 **미확인 · 질의 대상** — 위 「금지 문언에 해당하지 않음」은 **문언 해석**이며, 「모델로 첨부를 읽어 서류를 뽑는 것이 승인된 범위인가」를 동원님이 명시적으로 결정한 기록은 찾지 못했습니다. `12_제품방향:90-95` 는 「미결」로 열려 있습니다.
- 질문 문장 1개: **「첨부 텍스트에서 서류명을 뽑는 데 LLM 을 쓰는 것을 승인하십니까 — 출력은 원문 부분문자열 대조를 통과한 항목만 화면에 올린다는 조건으로」**

---

## 5. 권고 1개

### 권고 — **정답 세트 10건을 먼저 만들고, 그 위에서 「갈래 1(파서 도입) → 갈래 3」 순으로 간다. 파서 도입 여부 결정을 이번 주 안에 받는다.**

1. **정답 세트 10건 작성** — `07_표본조사:290-301` 의 10건과 `:305` 스키마를 그대로 씁니다. 코드 변경 0건.
2. **항목 단위 측정기 신설** — 기존 「1건 이상」 지표와 **다른 줄로** 출력합니다.
3. **파서 도입 결정을 받습니다** — 미도입이면 B형 13건은 구조적으로 영구 미커버이며, 제품 방향(`12_제품방향:81-86`)과 정면 충돌합니다.
4. 도입 승인 시 **규칙 기반으로 먼저 B형을 재고, 그 결과를 baseline 으로** 모델 경로를 붙입니다.

**근거**
- `12_제품방향:94` 가 「정답 세트 없이 만들면 잘 되는지 알 수 없다」고 순서를 못 박았고, `07_표본조사:307` 이 「정답 10건이 나오기 전에는 규칙/모델 선택을 하지 않는다」로 방침을 고정했습니다 `[인용]`.
- 현재 유일한 지표가 이진값이라 어떤 갈래를 택해도 **개선을 잴 수 없습니다** `[관측]`.
- B형 57% 는 파서 없이는 어느 갈래로도 열리지 않습니다 — 모델을 붙여도 입력이 없습니다 `[관측]`.
- 갈래 1 이 먼저인 이유: A형 8/8 이 이미 결정적으로 동작하고 항목마다 원문 근거를 답니다(`:461`). 모델을 먼저 넣으면 이 근거 구조를 잃습니다 `[관측]`.

**이 권고가 틀릴 수 있는 조건**
- 정답 10건 작성이 결선(10월) 준비를 잠식하는 경우 — **A형 3 + B형 3 + C형 1 = 7건**으로 줄여 착수. 난이도 상한(#13)과 「정답이 없음」(#19)은 반드시 포함.
- 파서 도입이 **불허**로 결정되는 경우 — 권고 무효. 제품 범위를 「A형 + 첨부 안내」로 재정의해야 하며 이는 제품 판단이라 제 반환 범위 밖입니다.
- 첨부 텍스트 품질이 실측보다 나쁠 경우 — HWP 표 90개가 `<표>` 로 소실된 이력이 있습니다(`07_표본조사:214`). 표 복구 실패 시 갈래 3 을 앞당길 근거가 됩니다.

### 참고 — 파서 도입/미도입 두 갈래

| | 도입 | 미도입 |
|---|---|---|
| 얻는 것 | B형 13건(57%)의 목록 접근 경로가 열림 | 「표준 라이브러리만」 성질 유지, 배포·기동 단순 |
| 잃는 것 | 외부 의존성 3~4개, 기동 복잡화, 확장자 위장 대응 필요 | B형 13건 영구 미커버 — 핵심 모듈이 A형 8건에만 동작 |
| 이미 검증된 조합 | `pdfminer.six`(폴백 `pypdf`) · `pyhwp`(`hwp5html`) · HWPX 는 표준 `zipfile`+정규식 — 표본 26/26 성공 | — |
| 오늘 확인한 자산 | `/tmp/onbid-survey/venv` 가 **아직 살아 있음** — `pypdf 6.17.0`·`pyhwp` import 성공, `hwp5txt`/`hwp5html` 바이너리 존재 `[관측]` | — |
| 🔴 주의 | 그 venv 는 `/tmp` 라 **재기동 시 소멸**합니다. 첨부 원본 26개와 표본 HTML 23개도 같은 위치입니다 `[관측]` | — |

---

## 6. 내가 확인하지 못한 것 (미검증)

| # | 항목 | 상태 | 이 경계를 닫는 최소 probe |
|---|---|---|---|
| 1 | A형 8건의 **항목 단위** 정확도 | 미측정 — 정답 세트 부재 | 정답 1건(#01)만 손으로 만들어 오늘 출력 12개와 대조. 30분 이내 |
| 2 | B형 11건 첨부 안 목록을 현행 어휘로 몇 % 잡는지 | 미측정 | `/tmp/onbid-survey/venv` 가 살아 있는 동안 26개 텍스트에 `find_doc_names()` 를 돌려 후보 수만 셈 |
| 3 | 열린 패턴 2개의 오탐 규모 | 미측정 — §2-4 는 1줄 반례만 | 같은 26개 텍스트에서 두 패턴 매칭만 뽑아 분류 |
| 4 | 두 가드를 **각각** 무력화했을 때 표본 수치 | 미실행 | `/tmp` 사본에서 각각 1회 + `measure_sample_23.py`. pyc 캐시 주의 |
| 5 | LLM 출력 미사용이 **의도**인지 미완인지 | 미확인 | 동원님께 질문 1개 |
| 6 | 모델 경로 승인 범위 | 미확인 | §4-4 질문 문장 |
| 7 | 라이브 온비드 동작 | 미검증 — 오늘 네트워크 호출 0회 | 공고상세 1건 실호출 |
| 8 | `/tmp` 자산의 잔존 기간 | 미확인 | 레포 밖 영구 경로 복사 여부는 동원님 결정 |
| 9 | 표본 23건의 전체 분포 대표성 | 미검증 — 층화추출 아님 | 이미 한계로 기재됨. 지우고 인용하지 않았음 |

---

<sub>**증거 등급** — 「오늘 실행」 수치는 이 세션에서 명령을 실행해 얻은 **실측**입니다. `file:line` 만 붙은 것은 **문서·코드 기재 사실**입니다. 「미측정」·「미확인」은 근거를 찾지 못한 것이며 부존재 판정이 아닙니다 — 부존재로 적은 것은 전수 grep 을 실행한 §3-1과 §1-3뿐입니다.</sub>
