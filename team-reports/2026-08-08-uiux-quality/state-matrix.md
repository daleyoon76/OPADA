# 상호작용/반응형 상태 매트릭스 — 2026-08-08

## 상호작용 상태 (담당: interaction-auditor)

### P0

- 없음.

### P1

- **관심 공고 빈 상태 도달 불가**: ux-audit.md P0-3과 동일 이슈(`app.js:378-397`).
  - 처리 상태: **이번에 "예시" badge로 완화** (완전 해결은 아니며, empty state 자체는 여전히 도달 불가 — 다음 이터레이션 후보)
- **포커스 소실**: 체크박스/별표 클릭 시 재렌더로 키보드 포커스 소실(`app.js:1407-1415, 655-664`).
  - 처리 상태: **보류**
- **확인 없는 삭제**: 초기화 버튼(`index.html:103`, `app.js:1405`)이 확인 없이 즉시 삭제 동작.
  - 처리 상태: **이번에 `confirm()` 가드 추가 완료**
- **요청 경합**: 샘플 불러오기/분석 중 요청 경합, fetch 취소 로직 없음(`runAnalysis`, `app.js:1316-1359`).
  - 처리 상태: **이번에 `analysisRequestId` 토큰 도입으로 해결 완료**
- **죽은 변수**: `analysisTimer`(`app.js:426,1317,1371-1373`) 죽은 변수, 이중클릭 방지 로직 실질 미작동.
  - 처리 상태: **이번에 `analysisRequestId` 도입하며 제거 완료**

### P2 (보류)

- 별표 버튼 터치 타깃 30×30px (44px 권장 미달)
- 로딩 진행 표시가 매끄럽지 않음
- 복사 피드백 성공/실패 스타일 구분 약함

### 강점 (변경하지 않음)

- URL 미입력/형식오류/분석실패 3분기 명확(`app.js:674-691, 762-780`)
- prepare 버튼 disabled + 텍스트 이중 신호 명확

## 반응형 상태 (담당: responsive-tester)

### P1

- **P1-1**: `.preview-grid`(`styles.css:368-373`) / `.coach-facts`(`styles.css:448-453`)가 900px 모바일 스택 규칙(`styles.css:1194`)에서 누락되어 390px에서 라벨/값이 심하게 눌림.
  - 처리 상태: **이번에 모바일 스택 규칙에 추가 완료**
- **P1-2**: 브레이크포인트가 900px 단일점뿐, 태블릿용 별도 규칙 없음.
  - 처리 상태: **보류(P1-1 해결로 부분 완화됨)**

### P2 (보류, 미확정)

- `watch-actions`(`styles.css:1035-1040`) flex-wrap 미지정 — 데이터가 늘면 위험, 현재는 안전
- textarea `overflow-wrap` 명시 없음 — 위험 낮음(확인됨)
- board-status 3열 여유폭이 900~960px 구간에서 타이트 — **실측 필요(미검증)**

## 요약

이번 이터레이션에서 상호작용 P1 5건 중 4건 해결(포커스 소실만 보류), 반응형 P1 2건 중 1건 해결. P2는 전량 보류. board-status 3열 900~960px 구간과 태블릿 중간 폭은 실측하지 못했다 (`verification.md`의 미검증 항목 참고).
