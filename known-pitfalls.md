# Known Pitfalls — Hometest_op

## 실행 환경
- `src/server.py`는 표준 라이브러리만 사용하는 순수 Python HTTP 서버입니다 (외부 의존성 없음). `python src/server.py`로 바로 실행됩니다.
- macOS 환경에서의 기동·온비드 분석·AI 호출은 2026-08-03 기준 미검증입니다 (기존 실행 확인 기록은 Windows 경로 기준). 다른 OS에서 처음 돌릴 때는 먼저 기동 확인부터 하세요.

## 외부 API
- 공공데이터포털 인증키(`ONBID_API_SERVICE_KEY`)가 없으면 온비드 공개 원문 분석만 동작합니다. 승인 직후 일부 API가 403/`NODATA_ERROR`를 반환할 수 있으나 원문 분석 결과는 계속 표시됩니다 — API 실패를 전체 실패로 착각하지 마세요.
- AI 코치는 `local_ai_coach` → `gemini_ai_coach`(`GEMINI_API_KEY`/`GOOGLE_API_KEY`) → `vertex_ai_coach`(gcloud 토큰) 순으로 폴백합니다. 인증키/토큰이 없어도 `local_ai_coach`로 동작은 계속됩니다.
- `local_ai_coach`의 `has_docs`·`has_proxy_or_joint`·`has_direct_submit`는 **`notice["docChecklist"]`를 근거로 삼습니다.** 문자열 매칭으로 되돌리면 구분명만 있는 표를 추출 성공으로 통과시킵니다(2026-09-07 15건 실측). 이 함수를 고칠 때는 `tests/test_docs_extraction.py`의 B1 축을 함께 보세요.
- 시연용 샘플 URL은 입찰기간 종료 후 온비드 페이지가 사라지거나 상태가 바뀔 수 있습니다. 결선 시점 직전에 유효한 URL로 다시 확인하세요.

## 문구/표현
- 발표자료·시연 멘트에는 `CLAUDE.md`의 금지표현 12종을 쓰지 않습니다. 신청서 단계부터 유지된 규칙이며, 어겨진 이력이 있어 재작성 시 재검사가 필요합니다.

## HTML 파싱
- `values_after_label()`은 `<span class="tit01">라벨</span>` 다음 값을 집는데, 온비드 좌측 내비게이션에도 같은 라벨이 있으면 **메뉴 문구를 먼저 집습니다**. 2026-09-02에 `재산유형`이 이 함정에 걸려 화면에 "중메뉴 펼치기 / 매각"으로 표시됐습니다.
  - 이 함수는 14개 필드가 함께 씁니다. hidden input에 같은 값이 있으면 그쪽을 먼저 보세요.
  - 검증할 때는 재산유형을 바꿔 가며 확인하세요. 압류재산·기타일반재산·파산자산에서 각각 다른 hidden input이 채워질 수 있습니다.
- 파싱 결과는 API 응답만 보지 말고 **화면까지** 확인하세요. `assetType`은 `app.js`의 3곳(미리보기·요약·복사 문구)에 그대로 렌더됩니다.

## 제출서류 추출

- **온비드 「입찰방법」 절은 서류명 탐색 대상이 아닙니다.** 그 절의 공통 도움말이 표본 23/23에서 `위임장`·`대리입찰신청서`·`공동입찰참가신청서`를 담고 있어, 섹션을 분리하지 않고 키워드로 뽑으면 **전건 오탐**입니다. 가드는 두 겹입니다 — `DOC_SEARCH_SECTIONS` 절 배제와 `strip_tooltips`. 둘 중 하나만 남아도 통과하므로 **각각 따로 검사**해야 합니다.
- **온비드 「제출서류」 표의 서류명 칸은 실제 서류명이 아닌 경우가 많습니다.** 표가 있는 표본 15건 전건이 구분명 그대로(`공동입찰서류`)이거나 `-` 또는 `보증서등(공고문 확인)`이었습니다. 이 행을 서류로 세면 화면이 「원문에 서류가 보입니다」라고 거짓 안내합니다(2026-09-07 실측 15건). `generic` 플래그로 갈라야 합니다.
- **재산유형으로 서류 위치를 미리 가를 수 있습니다.** 압류·국유 = 본문(A), 공유·기타일반·수탁 = 첨부(B), 파산 = 원문에 없음(C). 표본 23건 예외 0건이나 **층화추출이 아니므로 전체 분포 추정치는 아닙니다.**
- 서류명 어휘는 **닫힌 목록**입니다. 목록에 없는 서류는 조용히 빠지는 것이 아니라 「못 뽑음」으로 보고돼야 합니다. 긴 이름을 먼저 두지 않으면 `법인 인감증명서`가 `인감증명서`로도 중복 계상됩니다.
- 조건 키워드만 보면 축이 부풀려집니다. 표본 19는 `공동입찰허용여부 [■ 불허용]`이라 키워드로는 걸리지만 서류 분기가 아닙니다. 부정 문맥 가드가 필요합니다.

## 가격 표시

- **최저입찰가격이 0이면 「비공개」입니다.** 온비드 물건상세 hidden input `lowstBidPrcHideDivCd`가 `0001`이 아니면 온비드 자신도 화면에 「비공개」를 찍습니다(온비드 페이지의 자체 스크립트로 확인). 공개 여부는 이용기관이 정합니다(2026-09-07 캠코 회의). `lowstBidPrc = 0`을 그대로 화면에 내보내면 안 됩니다.

## 온비드 URL
- 물건상세는 POST 이동이라 주소창에 파라미터가 남지 않습니다. GET으로 열려면 `onbidCltrno`·`pbctCdtnNo`·`onbidPbancNo`·`pbctNo` **4개**가 필요하고, 하나라도 빠지면 온비드가 HTTP 500을 반환합니다.
  - ⚠️ 2026-09-02 기재는 「`cltrHisNo` 포함 5개가 모두 필요」였으나 **오기**입니다. 2026-09-08 실측(파라미터를 하나씩 빼며 5회 요청)에서 `cltrHisNo` 만 빼면 HTTP 200·722,685바이트로 정상 개통했고, 나머지 4개는 각각 빼면 HTTP 500이었습니다.
  - 🔴 **`itemDetail` 링크가 열리는지는 사용자가 무엇을 붙여넣었느냐에 달렸습니다.** `build_related_urls()` 는 4개를 모두 싣지만, `pbctCdtnNo` 는 **물건상세 페이지에만** 있습니다.
    - **물건상세 URL 입력** — HTTP 200(724,351바이트, 2026-09-08 실측 1건). 이 경로에서만 「죽은 링크가 아니다」가 참입니다.
    - **공고상세 URL 입력** — `pbctCdtnNo` 가 없어 URL 에서 떨어지고 **HTTP 500** 입니다(2026-09-08 실측 2/2).
    - 표본 23건은 **전부 공고상세**이고 `pbctCdtnNo` 보유는 **0/23** 입니다. 공고상세를 붙여넣는 사용자에게 나가는 링크는 500 입니다.
- 물건 목록은 마감임박 순이라 페이지 번호가 시간이 지나면 밀립니다. 페이지 번호로 특정 물건을 다시 찾지 말고, 같은 세션에서 조건에 맞는 행을 찾는 즉시 상세로 들어가세요.
- 목록 행의 `.btn_like[data-id]`가 `onbidCltrno`와 같습니다.
