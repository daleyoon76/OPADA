# 시연 실행 순서 — 2026-09-03(목) 16:00 캠코 화상회의

- 근거 실측: 2026-09-02 11:00~11:20 KST, 현재 작업 기기(macOS)
- 대상 URL: `03_시연용_실URL_20260902.md`

## 1. 사전 점검 (회의 30분 전)

| 순서 | 명령·동작 | 통과 기준 |
| --- | --- | --- |
| 1 | `gcloud auth application-default print-access-token` | 토큰이 출력된다 |
| 2 | `python3 src/server.py` | 기동 후 로그에 오류가 없다 |
| 3 | 브라우저에서 `http://127.0.0.1:8975/` | 화면이 뜬다 |
| 4 | 임대 URL 1건 분석 | 2초 내외에 결과가 뜨고 AI 상태가 연결로 표시된다 |

- 2026-09-02 기준 위 4단계는 모두 통과했습니다.
- 분석 소요는 임대 1.7~2.2초, 매각 2.2~2.4초로 실측했습니다.

## 2. 시연 흐름 (10분)

1. **임대 건 먼저 보여 줍니다** — 원인재역 공간임대
   - 검토 목적을 `임대/대부`로 두면 화면에 "목적 확인"이 뜹니다.
   - 공고/물건명, 유형, 입찰기간, 가격/보증금이 한 화면에 모입니다.
2. **준비 보드를 만듭니다**
   - 체크리스트 5건이 생성됩니다.
   - 미확인 항목과 담당기관 확인 질문이 따로 표시됩니다.
3. **매각 건으로 바꿉니다** — 김해 내덕동 주/상용건물
   - 검토 목적을 `매각/공매`로 바꿔야 "목적 확인"이 뜹니다.
   - 목적을 바꾸지 않으면 "목적 불일치" 안내가 뜹니다. 이 화면 자체를 기능으로 설명해도 됩니다.

## 3. 확인 요청으로 넘어가는 지점

원인재역 건의 가격이 화면에 `0`으로 표시됩니다.

- 원문이 회차 선택형이라 단일 최저입찰가격을 제공하지 않기 때문입니다.
- 이 지점을 사전 공유 자료 5절의 세 번째 질문으로 자연스럽게 연결할 수 있습니다.

## 4. 장애 대응

모두 2026-09-02에 실제로 재현해 확인한 결과입니다.

| 상황 | 화면 동작 | 검증 | 대응 |
| --- | --- | --- | --- |
| gcloud 토큰 실패 | 규칙 기반으로 자동 전환. 준비보드 5건·미확인 3건으로 **화면 구성이 동일** | 실측 | 그대로 진행 |
| 온비드 아닌 주소 입력 | "현재 MVP는 onbid.co.kr URL만 분석합니다" 안내 | 실측 | 주소 다시 붙여넣기 |
| 파라미터 없는 주소 입력 | 공유하기로 주소를 복사하라는 안내 | 실측 | 안내대로 재입력 |
| 빈 값으로 분석 | "온비드 URL을 입력해야 합니다" 안내 | 실측 | 주소 입력 |
| 마감된 물건 입력 | **경고 없이 진행 중인 공고와 똑같이 표시됨** | 실측 | 아래 주의 참고 |

- 오류 3종은 모두 HTTP 400과 한국어 안내로 처리되며 화면이 깨지지 않습니다.
- 토큰은 45분 캐시 후 재발급합니다. 회의가 1시간이면 도중 1회 재발급이 일어날 수 있습니다.

### 주의: 마감 경고가 없습니다

- 2026-07-08에 마감된 옛 샘플 공고를 넣어도 화면은 "목적과 입력 공고 유형이 맞습니다"로 표시하고, 입찰기간만 그대로 보여 줍니다.
- 56일이 지난 공고인데 진행 중인 공고와 화면상 구분되지 않습니다.
- 시연 중 캠코 측이 임의의 옛 주소를 넣어 보자고 하면 이 부분이 드러납니다. 미리 알고 계시는 편이 좋습니다.

## 5. 현재 환경 상태 (2026-09-02 실측)

| 항목 | 상태 |
| --- | --- |
| gcloud 토큰 | 정상 |
| `GEMINI_API_KEY` / `GOOGLE_API_KEY` | 미설정 (없어도 동작) |
| `ONBID_API_SERVICE_KEY` | 미설정 (보조 경로라 없어도 동작) |
| 외부 접속 주소 | 없음 — 화면 공유로 진행 |
