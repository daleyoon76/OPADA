# 공개 접속 주소 — ngrok 고정 도메인

- 작성: 2026-09-08
- 대상 기기: 맥미니 (이 저장소가 있는 기기)
- 성격: 시연·외부 확인용 임시 공개 경로다. 배포가 아니다.

## 결론

- 공개 주소는 **`https://overripe-astrology-impure.ngrok-free.dev`** 이다.
- **재기동해도 주소가 바뀌지 않는다.** 무료 요금제에 고정 도메인 1개가 계정에 배정된다.
- 이전 문서의 「재기동마다 주소가 바뀐다」 기재는 **더 이상 사실이 아니다**(2026-09-08 실측).

## 설정 (1회, 완료됨)

- `ngrok` 3.39.11 — `brew install --cask ngrok` 로 설치돼 있다.
- authtoken 을 2026-09-08 에 등록했다.
  - 저장 위치 `~/Library/Application Support/ngrok/ngrok.yml`
  - **기기 단위 설정이다.** Claude Code 세션과 무관하게 유지된다.
  - 토큰 값은 이 저장소 어디에도 적지 않는다.

## 기동

```
python3 src/server.py          # 로컬 서버 (127.0.0.1:8975)
ngrok http 8975               # 터널
```

- 두 프로세스가 **모두** 떠 있어야 공개 주소가 살아난다.
- 지금은 상시화(launchd) 를 하지 않았다. 프로세스가 죽으면 주소는 응답하지 않는다.
- 발급 주소 확인 — `curl -s http://127.0.0.1:4040/api/tunnels`

## 검증 기록 (2026-09-08)

| 항목 | 결과 |
| --- | --- |
| 1차 기동 (PID 40952) | `https://overripe-astrology-impure.ngrok-free.dev` |
| 종료 후 재기동 (PID 40955) | **같은 주소** |
| 외부 왕복 | HTTP 200 · ngrok 엣지 IP `54.65.101.67` |
| 내용 일치 | 로컬 원본과 sha256 동일 (`17c344f2…` · 7,799 bytes) |

- 판정 근거는 전부 `[관측]` 이다. 재기동 전후로 ngrok PID 가 실제로 바뀐 것을 확인했다.

## 유의점

- **브라우저로 열면 ngrok 안내 페이지가 먼저 뜬다.** `Visit Site` 를 눌러야 화면이 나온다.
  - 무료 요금제의 고정 동작이다. curl·API 호출은 그냥 통과한다.
  - 외부인에게 주소를 줄 때는 이 한 단계를 함께 안내한다.
- **서버에 인증이 없다.** 주소를 아는 사람은 누구나 분석을 돌릴 수 있다.
  - 지금은 이 기기에 `ONBID_API_SERVICE_KEY` 가 없어 실질 피해가 없다.
  - 🔴 키를 넣고 공개해 두면 **남이 우리 일 1,000건 한도를 소비한다.** 그때는 접근 제한을 함께 건다.
- SSRF 는 닫혀 있다 — `src/server.py:687` 이 `onbid.co.kr` 외 호스트를 거부한다 `[관측]`.

## 운영계정 신청과의 관계

- 공공데이터포털 운영계정 신청의 블로커는 필수 항목 **서비스 URL** 이었다.
- 이 주소로 그 칸을 채울 수 있다. 절차는 `06_공공데이터_인증키_발급안내_20260902.md` 를 본다.
- 다만 신청 전에 두 가지를 먼저 정한다.
  - 상시 가동 여부 — 활용사례에 적은 URL 은 남이 열어 볼 수 있어야 한다.
  - 인증 추가 여부 — 위의 무인증 노출 문제다.
