# Changelog

(아래 기준에 해당하는 항목만 누적 기록. 작업 일지가 아님)
(기준: 마일스톤/Phase 완료, 이슈 Close, 기능 전면 구현, 아키텍처·DB 결정, 반복 버그 근본 수정, 성능·보안 개선)

## 2026-08-03

- KAMCO Startup TechBlaze 모집부문1 예선(서류) `선정` — 10월 결선 발표평가 대상 확정.
- `supporting-programs` 레포에서 KAMCO Startup TechBlaze MVP를 `Hometest_op` 독립 레포로 분리 (서류 합격에 따른 분리).
- Harness 세팅 완료: `CLAUDE.md`/`architecture.md`/`plan.md`/`to-do.md`/`spec-sync-rules.md` 구성, `plan.md`는 `docs/60_본선준비` 계획 반영.

<!-- arch-review: 2026-08-03: 초기 세팅 -->

## 2026-08-08

- 온비드 물건상세 URL에 필수 파라미터가 없을 때 500 에러를 그대로 노출하던 문제를 근본 수정 — 사전 검증으로 400 + "공유하기 → URL 복사" 안내 메시지로 전환.
- 3팀(UIUX품질팀) 소집해 MVP 화면 전면 정리: notice-fit/analysis-preview 패널 통합으로 정보 중복 제거, badge/강조색 체계 재정립, 포커스 관리·요청 경합 등 상호작용 버그 수정, 관심 공고 화면 실제/예시 데이터 시각적 구분.

<!-- arch-review: 2026-08-08: UI/UX 전면 정리 -->

## 2026-09-02

- 화면에 표시되는 재산유형이 온비드 내비게이션 문구("중메뉴 펼치기")로 채워지던 파싱 결함을 근본 수정 — 라벨 기반 HTML 스크래핑보다 상세 페이지 hidden input(`scrnCltrPrptDivNm`)을 우선 참조하도록 변경. 압류재산·기타일반재산·파산자산 4건으로 회귀 0건 확인.
- 온비드 물건상세를 GET으로 여는 파라미터 조합을 확정 — `onbidCltrno`·`cltrHisNo`·`pbctCdtnNo`·`onbidPbancNo`·`pbctNo` 5개 전부 필요(누락 시 온비드가 500 반환). 시연용 실 URL 확보 절차가 성립했다.
- 공공데이터 API 6종의 활용신청 경로를 엔드포인트명 대조로 확정하고, 캠코로부터 서비스별 일 허용 건수 100,000건을 확인.

<!-- arch-review: 2026-09-02: 파싱 소스 우선순위 변경 -->

## 2026-09-06

- 마감된 공고에 "입찰 마감 지남" 사실 경고 표시 기능 구현 — `src/server.py`의 `bid_deadline_passed()`가 입찰기간 종료값/마감일시를 분석 시각과 순수 날짜 비교, 과거면 준비 보드 알림 맨 위에 경고를 추가. 입찰 여부·법률·수익성 판단은 넣지 않음(가드레일 준수). 이전까지 마감 공고가 진행 공고와 화면상 구분되지 않던 발견사항을 해소.
- e2e 테스트(`tests/public-data-status.spec.js`)를 08-08 UI 개편 이후 현행 DOM(`#source-notes` "보조 데이터:")에 맞게 수정하고, 공공데이터 인증키 유무에 따른 조건부 단언으로 전환. 키 없는 환경에서 로컬 러너 통과 확인.

<!-- arch-review: 2026-09-06: 마감 경고 기능 추가 -->

## 2026-09-07

- 공공데이터 오픈 API가 2026-07-04부터 두 달간 데이터를 반환하지 않던 문제를 근본 수정 — 온비드 물건상세 페이지의 hidden input은 물건관리번호를 하이픈 없이(`20260800045167`) 주는데 API는 4-4-6 하이픈 형식(`2026-0800-045167`)만 인정한다. 하이픈이 없으면 오류가 아니라 HTTP 200 + `NODATA_ERROR`로 응답해 인증·권한 문제로 오인됐다. `src/server.py`에 `api_cltr_mng_no()` 변환 함수를 추가하고 공공데이터 호출부에 배선. 시연 3건 전부 정상 조회로 전환(`missing_key`/`no_data` → `connected`), 화면 값은 불변이라 회귀 0.
- 오픈 API 6종의 실제 가용 범위를 실측 확정 — 인증키 6종 전부 승인 상태(신청 2026-07-04, 만료 2028-07-04, 개발계정). 재산유형 커버리지는 압류재산 54,642건·기타일반재산 16,842건·국유재산 3,636건·공유재산 356건·수탁재산 37건이며, 물건상세 1건에 84필드 중 66필드에 값이 있다. 「임대나 위탁 물건은 API에 없다」는 기존 추정을 폐기했다.
- KAMCO 온비드사업처 화상 멘토링 완료(2026-09-07 16:00) — 제품 방향을 「첨부파일에서 준비서류를 추출해 참고용 체크리스트로 제공」 하나로 좁혔다. 근거: 입찰자 입장의 서류 체크 로직이 온비드에 없고, 기관마다 서류·양식이 다양해 규칙화가 어렵다는 담당자 설명. 단 검증(validation)으로 만들어 입찰 진행을 막지 않는다는 제약을 함께 확정했다. 물건 검색·지도·예상 낙찰가·시세는 민간 업체가 포화 상태여서 추구하지 않는다.

<!-- arch-review: 2026-09-07: API 식별자 형식 변환 계층 추가 · 제품 범위 축소 -->

## 2026-09-08

- 제출서류 추출을 **절 경계 기반으로 전면 재구현** — 표본 23건 실측으로 구체 서류명 추출이 `0/23 → 10/23`(A형 8/8 · B형 2/13 · C형 0/2)이 됐다. 온비드 `<h2 class="tit">` 절로 자르고 「입찰방법」 절의 공통 도움말(표본 23/23에 위임장·대리입찰신청서·공동입찰참가신청서 포함)을 탐색에서 배제해 전건 오탐을 없앤 것이 핵심이다. 재산유형으로 A/B/C(본문·첨부·부재)를 사전 판정하고, 조건 축 10개로 서류를 분기하며, 서류마다 단계·원본/사본·부수·유효기간·제출방법·제출기한·발급처·원문 근거를 붙인다.
- **추출 실패를 성공으로 위장하던 경로를 근본 수정** — 온비드 제출서류 표의 서류명 칸이 구분명 그대로(`공동입찰서류`)인 15건을 `local_ai_coach`가 `has_docs=True`로 통과시켜 「원문에 공동입찰서류가 보입니다」라고 거짓 안내했다. 판단 근거를 문자열 매칭에서 `docChecklist`로 옮겨, 못 뽑으면 못 뽑았다고 화면에 그대로 표시한다.
- **첨부파일을 열 수단을 확보** — `fn_chkPdfRead` 5인자에서 `dnldFile.do` 다운로드 URL을 만든다. 종전에는 파일명만 추출하고 화면에 노출조차 하지 않았다. 표본 실측은 분모가 둘이라 갈라 적는다.
  - **공고 단위**: 첨부가 있는 공고 20건 중 다운로드 URL을 확보한 공고 `0/20 → 20/20`
  - **파일 단위**: 그 공고들의 첨부 파일 59개 중 URL이 붙은 파일 `0/59 → 59/59`
- 최저입찰가격을 0원으로 표시하던 문제를 근본 수정 — 온비드는 `lowstBidPrcHideDivCd != '0001'`이면 「비공개」로 표시하며 공개 여부는 이용기관이 정한다(2026-09-07 캠코 회의 확인 + 물건상세 HTML 실측).
- 2026-09-07 캠코 회의 조건을 설계 원칙으로 고정 — 서류 체크리스트는 참고용이며 적격 여부를 판정하지 않고, 항목이 비어 있어도 다음 단계를 막지 않는다. 진행 차단 지점을 전수 조사해 서류 기반 게이트가 0건임을 확인하고, 화면 문구를 「확인하셨습니까」로 통일했다.
- **공동입찰 가능 여부를 크롤링에서 읽어 조건 축에 물렸다** — 온비드 「입찰방법」 절의 구조화 필드이며 API 키 없이 표본 23/23 읽힌다. 값 span 이 라벨과 같은 `txt_01` 이라 기존 `label_value()` 로는 23/23 빈 문자열이었고, 전용 추출기 `bid_method_flag()` 를 신설했다. 라벨 뒤 툴팁에 「공동입찰참가신청서」가 23/23 들어 있어 절 전체를 훑는 구현은 전건 오탐이다.
  - 조건 축 `joint` 는 서류 줄 근거를 **덮지 않고 `or` 로 확장**한다. 표본 실측 `9/23 → 12/23`(증분 17·18·21)이고 반대 방향 불일치는 0/23 이라 기존 판정을 뒤집지 않는 순수 증분이다.
  - 명도책임은 **조건 축에 물리지 않는다** — 표본 3/23 뿐이고 값 어휘가 `매수인` 하나라 분기 근거가 없다. 값이 있을 때만 화면에 사실 한 줄로 표시한다.
  - 못 읽으면 화면은 「원문 확인」이다. 「불가능」·「해당없음」으로 단정하거나 행을 숨기지 않는다.
- 검증 체계를 3축으로 분리 — 순수 함수(`tests/test_docs_extraction.py`), 화면 노출(`tests/screen.spec.js`), 실 URL e2e. 축마다 음성 대조를 같은 수로 둔다. 단언 수는 게이트 실행 출력이 SoT 이고, 2026-09-08 05:05 실측은 순수 함수 **141단언·19축** · 화면 **2통과·9축**이다(`python3 tests/test_docs_extraction.py` 출력 「141/141 통과 · 축 19개 중 미커버 0개」 · `npx playwright test tests/screen.spec.js` 출력 「축 9개 중 미커버 0개」). 축 B8·B9·B10(입찰방법 값 추출 · joint 축 배선 · 명도책임)과 S9(공동입찰·명도책임 화면)가 이번에 늘어 119·16 과 8 에서 옮겨 왔다.
  - 이전 판이 적었던 킬러 변이 전수 검증 주장(서버 22 · 화면 17 합계)은 **미재현**이다 — 1차 독립 리뷰가 그 변이 목록을 산출물에서 찾지 못해 재현 불가로 판정했다. 실측으로 닫힌 것은 리뷰가 지정한 킬러 변이 4개(P1-1~P1-4)가 `/tmp` 사본에서 4/4 `rc≠0` 인 것까지다.

<!-- arch-review: 2026-09-08: 제출서류 추출 계층 신설(split_sections → build_doc_checklist) · 검증 3축 분리 -->
